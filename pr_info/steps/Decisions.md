# Decisions

## Directory Deletion Tool

**Decision:** Use `mcp__filesystem__delete_this_file` for deleting the empty `tests/utils/vscodeclaude/` directory.

**Rationale:** Preference for MCP tools over bash commands, consistent with project conventions in CLAUDE.md.
