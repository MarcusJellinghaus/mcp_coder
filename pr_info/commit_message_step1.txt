feat: add --timeout CLI flag for iCoder LLM inactivity timeout

Add configurable timeout for iCoder's LLM inactivity detection:

- Add `timeout` parameter to `RealLLMService.__init__()` with default
  of ICODER_LLM_TIMEOUT_SECONDS (300s)
- Add `--timeout SECONDS` argument to icoder CLI parser
- Plumb `args.timeout` from CLI through to `RealLLMService`
- Add tests for custom timeout passing and CLI argument parsing

Closes part of #769
