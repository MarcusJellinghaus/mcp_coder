feat: print MCP server versions in launcher scripts (#640)

Add `mcp-workspace --version` and `mcp-tools-py --version` calls to all
four launcher scripts (claude.bat, claude_local.bat, icoder.bat,
icoder_local.bat) after MCP tool verification succeeds.

Verified .mcp.json env var consistency: all env vars (MCP_CODER_VENV_PATH,
MCP_CODER_PROJECT_DIR, VIRTUAL_ENV, MCP_CODER_VENV_DIR) are set by all
four launchers before launch.
