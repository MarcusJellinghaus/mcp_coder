Step 3: pre-flight MCP server check with actionable diagnostics (#830)

Add _preflight_mcp_server helper in verification.py that distinguishes
unresolved ${VAR} residue from missing binary before the async launch,
using shutil.which to cover both absolute paths and PATH-resolved bare
executables (e.g. python, npx).

Use the helper inside _check_servers to short-circuit the live launch
with a category tag ("UnresolvedPlaceholder" or "FileNotFoundError")
and an actionable message naming the server and the offending field.

Enrich the launch-error fallback to include the resolved command path
and exception class name, so operators see why launch failed even when
pre-flight passed.

Tests updated/added in tests/llm/providers/langchain/test_mcp_health_check.py:
- Switch "python" to sys.executable where pre-flight must pass.
- Update test_server_failure and test_mixed_servers assertions for the
  new pre-flight "binary not found at ..." message; drop the mocked
  ConnectionError side_effect from the bad server in test_mixed_servers
  since pre-flight short-circuits before the async launch.
- Add placeholder-residue tests covering command, args, and env.
- Add launch-error fallback tests for non-OSError (ConnectionError) and
  for FileNotFoundError after pre-flight passes.
