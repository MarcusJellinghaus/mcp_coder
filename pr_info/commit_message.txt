docs: replace hand-copy instructions with mcp-coder init references

Update repository-setup documentation to reference the new
`mcp-coder init` command instead of manual file copying:

- claude-code.md: skills section now references `mcp-coder init`
  and `--just-skills` flag instead of "copy from mcp-coder"
- claude-code.md: knowledge base section says "deployed automatically"
  instead of "copy as-is"
- README.md: quick setup checklist adds `mcp-coder init` step and
  narrows manual config to CLAUDE.md and .mcp.json
- README.md: compatibility table note explains init-based deployment
  and no-overwrite behavior
