refactor(ci-wait): replace dot progress with periodic log messages

- Remove _show_progress() helper and print-based dot progress
- Log periodic OUTPUT-level messages every ~60s during CI polling
- Keep 15s poll interval unchanged
- Migrate PR-wait print() calls to logger.log(OUTPUT, ...)
- Migrate error/warning prints to logger.error()/logger.warning()
- Keep data-output print() for branch status report display
- Update tests to use caplog instead of capsys for log assertions
