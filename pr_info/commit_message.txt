config: rewrite verify_config to walk schema

Replace ~100 lines of hand-coded per-section checks in verify_config()
with a schema-driven loop over _CONFIG_SCHEMA.

- Add _get_section_data(), _verify_section(), _verify_wildcard_repos() helpers
- verify_config() now walks _CONFIG_SCHEMA, producing per-field entries
- has_error is True for type mismatches and missing required fields (not just invalid TOML)
- Delete _SECTION_ENV_VARS, _get_source_annotation(), _get_standard_env_var()
  (schema provides all needed info)
- Remove unused Optional import
- Update verify tests: switch from by_label dict to list filtering
- Add tests for type mismatch, unknown key, missing required, absent section
