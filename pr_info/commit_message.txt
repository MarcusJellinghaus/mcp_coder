config: update int, list, and langchain callers for native types

- Remove env var override loop in langchain/_load_langchain_config()
  since get_config_values() now handles env vars via schema
- Update test mocks: string "5" → native int 5 for max_sessions,
  JSON strings → native lists for setup_commands
- Update langchain env var override test to reflect schema-driven
  env var resolution
- Widen type annotations in test mocks to match get_config_values()
  return type (str | bool | int | list | None)
