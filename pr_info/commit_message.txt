refactor: delegate config_reader to pyproject_config (#640)

- Replace config_reader.py with thin wrapper that delegates to
  pyproject_config.get_formatter_config and re-exports
  check_line_length_conflicts
- Refactor formatters/__init__.py to use get_formatter_config instead
  of direct tomllib reads, switch from print to logger.warning
- All existing tests pass without modification
