feat(icoder): add newline hint below input area (#754)

Add a Static hint widget showing "\ + Enter = newline" below the
InputArea. The hint is visible when input is empty and hidden when
the user types text, toggled via on_text_area_changed handler.

Changes:
- app.py: yield Static hint in compose(), add on_text_area_changed handler
- styles.py: CSS for #input-hint (right-aligned, hidden class), InputArea border removal
- test_app_pilot.py: hint visibility tests (empty, has text, after submit)
