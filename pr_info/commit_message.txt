feat(icoder): auto-grow input area with content up to 1/3 screen height

- Remove max-height: 5 from InputArea CSS to allow dynamic sizing
- Add on_text_area_changed handler that sets height to
  min(line_count, screen_height // 3)
- Add test verifying input area height changes with multiline input
