feat(icoder): wire MCPManager and /info into execute_icoder (#741)

- Create MCPManager when provider=langchain and mcp_config is set
- Register /info command with runtime_info and mcp_manager
- Pass mcp_manager to RealLLMService constructor
- Ensure MCPManager.close() on exit via try/finally
- Add tests: MCPManager creation, cleanup, /info registration, error handling
