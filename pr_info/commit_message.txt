fix: resolve mypy strict-mode errors in test_pr_manager

- Remove unused `# type: ignore[attr-defined]` on `manager._repository`
  assignment (line 693) — attribute is now known to mypy after
  PullRequestManager gained the `_repository` attribute
- Add `dict[str, Any]` annotation to `response` variable (line 740)
  where empty `"nodes": []` prevented mypy from inferring the full type
