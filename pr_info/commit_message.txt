fix: add missing mocks for parse_claude_mcp_list in verify tests

Four verify command tests were failing because Step 3 of #748 added
Claude MCP server verification (parse_claude_mcp_list + prepare_llm_environment)
to execute_verify(), but these tests lacked the necessary mocks.

- test_verify_command.py: mock resolve_mcp_config_path → None to skip
  the MCP branch entirely (test doesn't exercise MCP behavior)
- test_verify_resolve_llm.py: same — mock resolve_mcp_config_path → None
- test_verify_orchestration.py (2 tests): add mocks for
  parse_claude_mcp_list and prepare_llm_environment since these tests
  intentionally enter the MCP branch with mcp_config="/fake/.mcp.json"
