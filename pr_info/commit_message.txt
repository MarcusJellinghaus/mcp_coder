Render detailed MCP tool listing with --list-mcp-tools flag (#639)

Add list_mcp_tools parameter to _format_mcp_section() that renders
each tool on its own indented line with descriptions aligned globally
across all servers. Update execute_verify() to pass the flag from
CLI args through to the formatter.

Update all test mock data to use (name, description) tuples and add
tests for detailed rendering, global alignment, missing descriptions,
failed servers, and orchestrator flag passthrough.
