feat(cli): add --add-system-prompts flag to prompt command

Add --add-system-prompts flag to the prompt parser that conditionally
passes project_dir to prompt_llm()/prompt_llm_stream() for system and
project prompt injection.

- Add --add-system-prompts store_true argument to add_prompt_parser()
- Wire prompt_project_dir in execute_prompt() for all 3 output paths
  (streaming, session-id, json)
- Without flag: project_dir=None (backward compatible, no prompts loaded)
- With flag: project_dir=str(project_dir) (prompts loaded and injected)
- Add 5 tests covering flag on/off, explicit project_dir, and all
  output format paths
- Update existing test assertions to include project_dir=None kwarg
