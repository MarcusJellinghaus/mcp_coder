feat: dynamic GitHub deps in reinstall_local.bat (#640)

Add tools/read_github_deps.py bootstrap helper that reads GitHub
dependency overrides from pyproject.toml and prints uv pip install
commands. Update reinstall_local.bat to:

- Replace hardcoded GitHub URLs with dynamic reading via
  read_github_deps.py
- Replace wrong-venv guard with silent deactivate at startup
