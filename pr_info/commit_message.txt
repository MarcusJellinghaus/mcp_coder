feat(langchain): prepend system messages in langchain provider

Add SystemMessage injection to the langchain provider:

- Add _build_system_messages() helper in __init__.py to convert
  system_prompt/project_prompt strings into SystemMessage objects
- Wire system_messages param through all internal functions:
  _ask_text, _ask_text_stream, _ask_agent, _ask_agent_stream
- Prepend system_messages to message lists in all code paths
- Add system_messages param to run_agent() and run_agent_stream()
  in agent.py with prepend logic
- Add _SystemMessage stub class to test conftest for isinstance()
  support in unit tests
- Add tests: _build_system_messages helper, text/stream prepending,
  agent prepending, end-to-end pass-through
