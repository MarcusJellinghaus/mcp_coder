refactor: remove formatter helpers from pyproject_config

Remove get_formatter_config() and check_line_length_conflicts() from
pyproject_config.py along with their test classes. These functions were
only used by the now-deleted mcp_coder.formatters package; equivalent
functionality lives in mcp-tools-py's utils/project_config.py.

Also removes the unused `from typing import Any` import.
