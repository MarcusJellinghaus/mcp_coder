rename from-github to install-from-github across all layers (#684)

Rename the `from-github` configuration key to `install-from-github` in
pyproject.toml, and the corresponding `from_github` Python identifier to
`install_from_github` across all source and test files. This includes:

- TypedDict field in types.py
- Function parameters and dict keys in helpers, workspace, session_launch,
  session_restart modules
- CLI flag --from-github to --install-from-github in parsers.py
- getattr lookups in coordinator commands
- All 14 test files updated to match new naming
