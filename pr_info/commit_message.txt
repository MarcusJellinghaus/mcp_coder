feat(icoder): add reset_session() to LLMService protocol

Add reset_session() method to LLMService protocol, RealLLMService,
and FakeLLMService. The method sets session_id to None, enabling
a fresh conversation on the next stream() call.

Tests added for both implementations and protocol conformance.
