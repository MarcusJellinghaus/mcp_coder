fix: add missing Returns section to _get_field_def docstring

Add Returns section to the `_get_field_def()` docstring in
user_config.py to satisfy ruff's DOC201 rule, which requires
documented return values for functions with non-None return types.
