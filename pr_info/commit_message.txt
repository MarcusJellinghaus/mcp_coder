test(icoder): update snapshots for input hint widget (#754)

Regenerate all 9 Textual SVG snapshot baselines to reflect the new
#input-hint Static widget added below the InputArea. The hint text
"\ + Enter = newline" is now visible in the initial state snapshot
and layout is updated across all snapshot tests.
