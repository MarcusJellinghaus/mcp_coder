refactor: extract _build_ci_error_details to ci_log_parser module

Move _build_ci_error_details from branch_status.py to ci_log_parser.py
to bring branch_status.py under the 750-line file size limit (764 -> ~587).

The function is logically related to CI log parsing and fits naturally
in the ci_log_parser module. A re-import in branch_status.py preserves
backward compatibility for all existing importers.
