Refactor implement workflow to use shared failure handling utilities

Replace private _get_diff_stat() and _format_elapsed_time() in
implement/core.py with shared imports from workflow_utils.failure_handling.
Simplify _handle_workflow_failure() to a thin wrapper that builds the
implement-specific comment and delegates to the shared handle_workflow_failure().

Update tests to verify delegation to shared handler rather than testing
internal label/comment logic (now covered by shared module tests).
