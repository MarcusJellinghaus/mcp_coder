feat: add faulthandler stderr safety net to CLI entry point

Enable faulthandler.enable(file=sys.stderr, all_threads=True) at the
top of src/mcp_coder/cli/main.py, before heavy imports. This is a
zero-cost safety net that captures native-level tracebacks on crashes
(e.g. segfaults) during CLI startup.

Add subprocess-based test to verify faulthandler is enabled after
importing the CLI module.
