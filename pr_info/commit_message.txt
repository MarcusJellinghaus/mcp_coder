feat(init): add _deploy_skills() and integrate into execute_init()

- Add _deploy_skills() that copies skills/, knowledge_base/, agents/
  from source into project_dir/.claude/ with never-overwrite policy
- Update execute_init() to validate --project-dir, resolve source dir,
  detect self-deploy (running inside mcp-coder repo), deploy skills,
  and conditionally create config (skip with --just-skills)
- Add TestDeploySkills tests (empty project, no-overwrite, counts,
  warnings, nested dirs, missing subdirs)
- Add TestExecuteInitWithDeploy tests (default flow, --just-skills,
  --project-dir, missing dir exits 1, self-deploy skip)
- Update existing TestInitCommand tests to mock deploy functions
