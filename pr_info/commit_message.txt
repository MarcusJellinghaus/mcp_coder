feat: add PROMPTS section to verify command and /info display

- Add PROMPTS section to `execute_verify()` showing system prompt,
  project prompt, Claude mode, and CLAUDE.md redundancy warning
- Add prompt paths display to iCoder `/info` command output
- Add `_prompt_source()` helper for formatting prompt source labels
- Add tests for verify prompt section (defaults, custom paths, redundancy)
- Add tests for /info prompt display (custom paths, shipped defaults)
