feat: add project_dir param to prompt_llm/prompt_llm_stream for prompt injection

Add `project_dir: str | None = None` parameter to both `prompt_llm()` and
`prompt_llm_stream()` in the interface layer. When provided, `load_prompts()`
is called to resolve system and project prompts, which are then passed
through to the provider functions.

Provider entry points updated with no-op parameters:
- `ask_langchain()` / `ask_langchain_stream()`: gain `system_prompt`, `project_prompt`
- `ask_claude_code_cli()` / `ask_claude_code_cli_stream()`: gain `append_system_prompt`, `system_prompt_replace`

Actual provider-side handling will be implemented in Steps 3-4.

Modified files:
- src/mcp_coder/llm/interface.py
- src/mcp_coder/llm/providers/langchain/__init__.py
- src/mcp_coder/llm/providers/claude/claude_code_cli.py
- src/mcp_coder/llm/providers/claude/claude_code_cli_streaming.py
- tests/llm/test_interface.py
- tests/llm/providers/claude/test_llm_sessions.py
