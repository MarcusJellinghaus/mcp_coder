refactor(create-pr): remove log_step, use OUTPUT level logging

- Remove log_step() wrapper function
- Replace ~20 call sites with logger.log(OUTPUT, ...)
- Use lazy %s formatting instead of f-strings for logger calls
- Add per-field PR summary output (PR Number, URL, Title, Base/Head Branch)
- Detail messages stay at INFO (hidden at OUTPUT threshold)
- Update tests to mock get_current_branch_name and detect_base_branch
  for PR summary field logging
