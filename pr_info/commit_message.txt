feat(icoder): integrate autocomplete into ICoderApp

Add public read-only properties to AppCore for registry and event_log,
exposing dependencies needed by InputArea's autocomplete behavior.

Wire CommandAutocomplete widget into ICoderApp.compose() between
OutputLog and InputArea. Pass registry and event_log from AppCore
into InputArea constructor so autocomplete state management works
end-to-end.

Changes:
- AppCore: add registry and event_log @property accessors
- ICoderApp: yield CommandAutocomplete() in compose()
- ICoderApp: construct InputArea with registry and event_log kwargs
