icoder: inactivity watchdog timeout for stream_subprocess

Replace one-shot threading.Timer with a watchdog daemon thread that
tracks last_activity time and kills the process when idle beyond
timeout_seconds. The timeout now resets on each yielded stdout line,
making it an inactivity timeout rather than a total-elapsed timeout.

- Add watchdog thread with stop_event for clean shutdown
- Reset last_activity on each yielded line in the output loop
- Create tests/utils/test_subprocess_streaming.py with real-process tests
- Update mock-based timeout test to work with watchdog pattern
