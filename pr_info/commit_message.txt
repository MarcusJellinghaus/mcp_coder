feat(icoder): fresh session by default, add --continue-session flag (#765)

Replace iCoder's auto-resume behavior with opt-in session continuation,
matching the prompt command's pattern.

Changes:
- Add --continue-session, --continue-session-from, --session-id args to
  icoder parser (mutually exclusive where appropriate)
- Replace auto-resume logic in execute_icoder() with priority-based
  continuation: --session-id > --continue-session-from > --continue-session
- Add 5 parser tests for the new icoder session continuation args
- Update test mock args to include new session attributes
