fix: remove unused type: ignore comments on ctypes.windll calls

Mypy performs platform narrowing after the `sys.platform != "win32"`
guard, making `ctypes.windll` a known attribute. The three
`# type: ignore[attr-defined]` comments on windll.kernel32 calls in
tui_preparation.py were flagged as unused by mypy's warn_unused_ignores.
