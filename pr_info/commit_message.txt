feat: wire MCP connection status into RuntimeInfo, TUI startup, and event log

- Add `mcp_connection_status: list[ClaudeMCPStatus] | None` field to RuntimeInfo
- Call `parse_claude_mcp_list()` in `setup_icoder_environment()` with graceful fallback
- Add `_connection_status_suffix()` helper in app.py for inline status display
- Update `on_mount()` to show "✓ Connected" or "✗ <error>" next to server versions
- Include `mcp_connection_status` dict in `session_start` event payload
- Add tests: env_setup (3), connection_status_suffix (4), app_pilot (2)
