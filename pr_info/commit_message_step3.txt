Add _find_claude_source_dir() runtime skill source resolver

Implement dual-lookup logic for locating Claude skill resources at
runtime:

1. Package resources via importlib.resources (wheel installs)
2. Repo-root .claude/ fallback (editable installs)

Validates that all three deploy subdirectories (skills/, knowledge_base/,
agents/) exist before returning. Exits with a detailed error message
listing tried paths and a reinstall hint if neither location resolves.

Add DEPLOY_SUBDIRS constant and _has_all_subdirs() helper for reuse
by the deploy step. Includes comprehensive tests covering both lookup
paths, failure mode, and partial-subdirectory rejection.
