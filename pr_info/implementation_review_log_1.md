# review-implementation review log 1

## Round 1 — 2026-08-29
**Findings**:
I'll gather context systematically. Starting with the knowledge base, issue, and plan files in parallel.`src/mcp_coder/llm/providers/langchain/__init__.py:271` — medium — an agent turn that ends without a terminal graph event (`agent.py:669-696`) still yields `done` with a session id whose history file was never written, so the next chained turn now raises: `icoder` is wedged until `/clear` and `create_plan`/`review`/`rebase` abort mid-workflow; accepted in `summary.md` but neither tested nor mitigated (e.g. by not reporting an id for a turn that persisted nothing).
`tests/llm/providers/test_langchain_session_guard.py:3` — low — module docstring justifies placing the file outside `tests/llm/providers/langchain/` because the conftest fixture would hide the guard, but `skip_langchain_history_guard` is opt-in, not autouse, so the rationale is stale and the file breaks the tests-mirror-src convention for no remaining reason.
`docs/cli-reference.md:167` — low — the `--store-response` / `--continue-session-from` lines still present that pairing as working, while under `--llm-method langchain` it now exits 1; only `docs/configuration/config.md` records the break.
`src/mcp_coder/llm/providers/langchain/__init__.py:748` — low — the module is now 748 lines against the 750-line CI limit (commit `0cd8717` had to shave 3 lines to fit), leaving no headroom for the next edit.
**Decisions**:
Verdict(decision='tasks', tasks=['In src/mcp_coder/llm/providers/langchain/__init__.py (around line 271, with the turn-completion path at agent.py:669-696): do not report a resumable session id for a turn that ended without a terminal graph event and therefore persisted no history file. Return the `done` result with the session id omitted (or otherwise marked non-resumable) so a subsequent chained turn does not raise on a missing history file and wedge icoder / abort create_plan/review/rebase mid-workflow.', 'Add a regression test covering the turn-ends-without-terminal-graph-event case: assert that no history file is written, that the result does not advertise a resumable session id, and that a following chained turn starts cleanly instead of raising.', 'Update docs/cli-reference.md around line 167 so the `--store-response` / `--continue-session-from` documentation states that this pairing is not supported under `--llm-method langchain` (exits 1), matching what docs/configuration/config.md already records.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-29
**Findings**:
I'll gather context systematically. Let me start with the knowledge base, issue, and plan files in parallel.`src/mcp_coder/workflows/create_plan/core.py:268` — medium — round-1's "omit an unresumable session id" fix does not deliver its stated goal for `create_plan`: prompt 1 runs with `session_id=None` and treats a missing `session_id` in the response as fatal, so a langchain agent turn with no terminal graph event still aborts the workflow (now at prompt 1, reported as "Prompt 1 did not return session_id" rather than the real cause), and no test covers this chain.

`src/mcp_coder/llm/providers/langchain/__init__.py:477` — medium — the "is this id resumable" policy is now implemented twice by different mechanisms: `run_agent_stream` decides it inside the `done` event, `run_agent` discards that field, and `_ask_agent` re-derives it with an independent `langchain_history_exists()` stat; the two rules agree today only by coincidence and can silently diverge (having `run_agent` return the `done` event's id would keep one decision point).

`src/mcp_coder/llm/providers/langchain/__init__.py:434` — low — the `Returns:` docstring states `session_id` is None "when the turn stored nothing", but the implemented rule is "when no history file exists": a resumed session whose turn stored nothing still returns the id (the `agent.py:699-702` comment states this correctly).

`docs/cli-reference.md:167` — low — "Not supported with `--llm-method langchain`" is over-broad: `--continue-session-from` pointed at an actual `~/.mcp_coder/sessions/langchain/<id>.json` still resolves a valid id via `extract_langchain_session_id` (`prompt.py:107`) and works; only `--store-response` files (`response_<ts>.json`) fail.

`tests/llm/providers/langchain/test_langchain_agent_stream_history.py:158` — low — `test_cancel_persists_nothing` dropped its `done["session_id"] == "s1"` assertion without replacing it, leaving the cancel path's new id-omission behaviour unasserted.

`src/mcp_coder/checks/branch_status.py:17` — low — pre-existing/environmental, not from this diff: `mcp_workspace.checks.branch_status_rendering` is absent, so `import mcp_coder` fails and the entire test suite (including this branch's new tests) cannot be collected locally; mypy reports no errors in the changed files, but the branch's tests could not be executed here.
**Decisions**:
Verdict(decision='tasks', tasks=["Fix src/mcp_coder/workflows/create_plan/core.py:268 so the round-1 'omit unresumable session id' behaviour actually works for create_plan: prompt 1 runs with session_id=None, so a missing session_id in its response must not be fatal — only treat a missing session_id as an error for turns that require resuming, and surface the real cause instead of 'Prompt 1 did not return session_id'. Add a test covering the create_plan chain when a langchain turn ends without a terminal graph event.", "Remove the duplicated 'is this id resumable' policy in src/mcp_coder/llm/providers/langchain/__init__.py: keep the decision in run_agent_stream's done event, have run_agent return that event's session_id instead of discarding it, and drop the independent langchain_history_exists() re-derivation in _ask_agent so there is a single decision point.", "Correct the Returns: docstring at src/mcp_coder/llm/providers/langchain/__init__.py:434 to describe the implemented rule ('None when no history file exists for the session'), not 'when the turn stored nothing' — a resumed session whose turn stored nothing still returns its id, as the agent.py:699-702 comment states.", 'Narrow the docs/cli-reference.md:167 wording: --continue-session-from pointed at a real ~/.mcp_coder/sessions/langchain/<id>.json still works under --llm-method langchain; only --store-response response_<ts>.json files are unsupported there. Update the note to say exactly that.', 'Restore coverage in tests/llm/providers/langchain/test_langchain_agent_stream_history.py:158: test_cancel_persists_nothing should assert the new id-omission behaviour on the cancel path (e.g. that done has no session_id / session_id is None) rather than leaving it unasserted.'], escalate_reason=None)
**Changes**:
applied
