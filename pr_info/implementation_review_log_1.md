# review-implementation review log 1

## Round 1 — 2026-08-29
**Findings**:
I'll gather context systematically. Let me start with the knowledge base, the issue, and the branch state in parallel.Let me read the rest of the issue and the knowledge base.Now let me compute the diff and read the plan files.`src/mcp_coder/icoder/services/llm_service.py:114` — medium — `inject_prompts=True` is the per-site mapping that preserves iCoder prompt injection, but no test asserts it; `tests/icoder/test_llm_service.py:334` only pins `project_dir` forwarding, so the "iCoder still injects the system and project prompts" acceptance criterion is untested (the `verify.py` counterpart is asserted at `tests/cli/commands/test_verify_orchestration.py:392`).
`tests/cli/commands/test_check_branch_status.py:89` — low — `test_execute_check_branch_status_read_only_success` and `..._llm_mode` newly patch `resolve_claude_cwd`, which by design cannot raise; this removes the only command-level coverage that the per-run context report fires on the read-only path (the very reason the call was hoisted out of the `--fix` block).
`src/mcp_coder/workflows/review/reviewer.py:130` — low — leftover `cwd = str(project_dir)` alias, then passed as `project_dir=cwd` at `:159`; the dead `execution_dir` fallback is gone but the second name for one directory survives, against the change's "one directory concept" goal.
`src/mcp_coder/workflows/review/reviewer.py:206` — low — same leftover `cwd = str(project_dir)` alias, passed as `project_dir=cwd` at `:224`.
`src/mcp_coder/checks/branch_status.py:17` — low — pre-existing/environmental, not caused by this diff: the installed `mcp_workspace` lacks `checks.branch_status_rendering`, so `import mcp_coder` fails; pytest collected 0 tests and mypy/pylint report only errors in files outside the diff. The branch's test suite could not be executed locally, so this review is static only.
**Decisions**:
Verdict(decision='tasks', tasks=["Add a test in tests/icoder/test_llm_service.py asserting that the LLM call at src/mcp_coder/icoder/services/llm_service.py:114 forwards inject_prompts=True, mirroring the existing project_dir assertion at tests/icoder/test_llm_service.py:334, so the 'iCoder still injects the system and project prompts' acceptance criterion is covered.", 'In tests/cli/commands/test_check_branch_status.py, restore command-level coverage that the per-run context report fires on the read-only path: instead of patching resolve_claude_cwd in test_execute_check_branch_status_read_only_success and ..._llm_mode, assert the context report is emitted (or let resolve_claude_cwd run and assert on its observable effect) so the reason the call was hoisted out of the --fix block stays tested.', 'Remove the leftover `cwd = str(project_dir)` alias at src/mcp_coder/workflows/review/reviewer.py:130 and pass `project_dir=str(project_dir)` directly at :159, keeping a single name for the one directory concept.', 'Remove the leftover `cwd = str(project_dir)` alias at src/mcp_coder/workflows/review/reviewer.py:206 and pass `project_dir=str(project_dir)` directly at :224.'], escalate_reason=None)
**Changes**:
rebase-needed
**Escalate reason**: rebase
