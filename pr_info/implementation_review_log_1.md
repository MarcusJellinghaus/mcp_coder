# review-implementation review log 1

## Round 1 — 2026-08-04
**Findings**:
I'll gather context systematically. Let me start with the knowledge base, the issue, and the branch state.Let me read the rest of the issue, the knowledge base, the summary, and compute the diff.The `main...HEAD` ref failed. Let me check git state.Local `main` doesn't exist; let me use `origin/main`.There's real implementation. Let me read the knowledge base and the new source files.The CLI now uses `assess_ci` but may have left `CIStatus` unused. Let me check.Let me verify the config-test docstring update and run the key quality checks.The pytest collection failed on an import error. Let me see the full error.Tests can't run here — `mcp_workspace` (a reference package) isn't importable in this sandbox, so conftest collection fails. That's an environment limitation, not a defect in the diff (the shim is unchanged on `main`). Let me verify the exit-code regression coverage.Let me check the docs changes against the issue's explicit recovery-column requirement.Let me verify the `core.py` size budget (an explicit acceptance criterion).`review/core.py` is under 600 — criterion met. Let me check the project's actual configured file-size limit, since a just-split test file lands at 602.I've completed my review. The branch contains real, faithful implementation of both gates plus policy helper, labels, config, docs, and thorough tests. I confirmed: `assess_ci` is the single implementation across all three callers with byte-identical CLI exit codes (regression-tested for every `CIStatus`); Gate 2 makes exactly one `collect_branch_status` call; the allowlist semantics, label wiring, `details` insertion, and `core.py` under-600 budget all hold. Tests could not be executed here because the `mcp_workspace` reference package isn't importable in this sandbox (a pre-existing shim unchanged on `main`), not a defect in the diff.

One substantive finding:

`src/mcp_coder/workflows/review/gates.py:108` — low — In `check_ci_proven_gate`, the `FAILED` verdict (→ `"ci"`/`17f-ci`, a determinably-red CI) reuses the same `detail` string as the undeterminable case: "could not prove CI ran green. Check the GitHub token and whether this repo has a CI workflow." That advice is misleading for a genuinely red CI (recovery is to fix the code, not the token), and "could not prove green" is contradictory when CI was proven red; the token/CI-exists wording should be scoped to the `ci_unknown` path only. Reachable in practice via `check_and_fix_ci`'s `ci.py:424/438` edges, and currently asserted-in by `test_gates.py::test_failed_is_the_existing_ci_reason`.
**Decisions**:
Verdict(decision='tasks', tasks=["In check_ci_proven_gate at src/mcp_coder/workflows/review/gates.py:108, split the detail message so the FAILED (determinably-red CI) verdict no longer reuses the ci_unknown wording: give FAILED a message indicating CI ran and failed with recovery guidance to fix the failing code, and scope the 'could not prove CI ran green / check the GitHub token and whether this repo has a CI workflow' text to the ci_unknown path only. Update test_gates.py::test_failed_is_the_existing_ci_reason accordingly."], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-04
**Findings**:
I'll gather context in parallel: knowledge base files, the issue, and pr_info.Let me read the rest of the issue, knowledge base, summary, and compute the diff.The git ref `main` isn't resolving. Let me check available branches and refs.Let me use `origin/main` for the diff base.There are real implementation changes. Let me read the knowledge base and the core source diffs.Now let me read the key source files and their diffs.Let me see the rest of the core.py diff, particularly the gate 2 wiring.Let me check the `_fail` signature and whether `CIStatus` is now unused in check_branch_status.py.Let me read the full dismiss-gate cascade in core.py as it now stands.Let me check the labels.json additions and the test files for gates.Let me look at test_core_gates.py and the exit-code regression test, plus verify the config test docstring update.Let me check the docs and remaining test diffs for completeness against acceptance criteria.Let me verify the `get_incomplete_tasks` signature to confirm gate 1's call is correct.The implementation closely matches the design. Let me run the focused tests and mypy to validate there are no mechanical failures.The pytest and mypy failures both stem from `mcp_workspace` not being importable in this environment (`import-not-found` at `checks/branch_status.py:17`), which cascades into every attr-defined/call-arg error — all on pre-existing lines the diff doesn't touch. Let me confirm `core.py:141` isn't newly added by this PR.Line 141 is pre-existing loop code, untouched by the PR. All mypy/pytest failures trace to the absent `mcp_workspace` module (environment limitation), not this PR's code. Let me verify the acceptance criterion that `core.py` stays under 600 lines.I've completed a thorough review of the branch against issue #1107's design.

**Context gathered:** knowledge base (SE principles, Python conventions), issue #1107 in full (both gates, all 15 decisions, constraints), `pr_info/steps/summary.md`, and the full diff against `origin/main`.

**Verification performed:**
- `ci_policy.assess_ci` — single implementation, correct allowlist semantics; CLI `_exit_code` and pre-`--fix` bail-out both delegate with `require_proven=False`; regression test covers every `CIStatus` × both flags with byte-identical exit codes.
- `gates.py` — Gate 1 mirrors `create_pr` error handling (skip on `FileNotFound`, block on any other exception), caps listing at 10, uses `get_incomplete_tasks(str(project_dir/"pr_info"))` with the correct default `exclude_meta_tasks=False`; Gate 2 makes exactly one fresh `collect_branch_status`, allowlist verdict mapping correct.
- `core.py` — Gate 1 at top of `body()`; Gate 2 folds into the dismiss cascade after `_after_steps` and *before* `_flush_round_log` (the push-then-block hazard the issue warns about is avoided); stays at 600 lines.
- `handoff._fail` — `details` inserted directly under the header, backward-compatible.
- `labels.json` — both labels with correct recovery commands and full `vscodeclaude` blocks; `config.py` field + `failure_labels` entries; docs (dev-process recovery column with `set-status`, cli-reference, workflow matrix) updated; test suites (gates, core gates, config, label config, handoff, exit-code regression) are behavior-focused and complete.

The mypy/pytest failures in this environment all trace to the `mcp_workspace` module being unimportable (`import-not-found` at `checks/branch_status.py:17`), cascading into pre-existing lines the diff never touches (`core.py:141`, `check_branch_status.py:154/299/301`, `create_pr/core.py:444`). The PR's own new modules (`ci_policy.py`, `gates.py`) type-check clean and add no new errors. These are pre-existing/environment issues, out of scope per the knowledge base.

NO FINDINGS
**Decisions**:
Verdict(decision='dismiss', tasks=[], escalate_reason=None)
**Changes**:
dismiss
