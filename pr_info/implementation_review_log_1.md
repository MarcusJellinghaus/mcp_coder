# review-implementation review log 1

## Round 1 — 2026-09-26
**Findings**:
I'll gather context first.Now the diff against main.`src/mcp_coder/install/_phases.py:155` — medium — `phase_overrides` now prints nothing when the resolved `local_path` has no `pyproject.toml` (or no `install-from-github` section); `tools/install.py` printed an explicit "skipping GitHub overrides (no pyproject.toml available)". This is the exact pitfall `installation.md` and `README.md` warn about for `--source git` without `--local-path`, and it is now silent in an otherwise fully command-echoing installer.
`tests/install/test_install_phases.py:74` — medium — `_run_install_check` drives the real `install()`, whose `ensure_system_uv()` is not stubbed, so these "dry-run" tests are not hermetic: without `uv` on PATH they execute a real `pip install uv` into the active venv or `sys.exit`. Probe-verified they pass here only because `uv` is on PATH.
`src/mcp_coder/utils/pyproject_config.py:154` — low — `get_install_extras` is annotated `str | None` but returns the raw TOML value; a mistyped `extras = ["dev"]` reaches `InstallConfig.extras` and then yields the garbage spec `mcp-coder[['dev']]`, or `AttributeError` in `phase_install_main`'s `config.extras.split(",")`. The strict read exists to surface target-repo mistakes — a type check belongs with it.
`src/mcp_coder/install/__init__.py:43` — low — `MCP_CODER_REPO`, `REPORT_BINARIES` and `REPORT_PACKAGES` are imported and listed in `__all__` but no caller imports them from the package (tests import them from `._env`); dead public surface against Decision 12's stated shape of `InstallConfig` + `install`.
`tools/reinstall_local.bat:13` — low — the repo-venv filter compares `%MCP_CODER_VENV_PATH%` with `%REPO%\.venv\Scripts` as literal strings, so a trailing separator or a short (8.3) path form defeats it and the driver becomes the `mcp-coder.exe` the install is about to rewrite — the lock Decision 20 exists to avoid.
`tests/workflows/vscodeclaude/test_session_setup_flow.py:98` — low — `_is_install` weakened from an exact script path to `"install" in argv`, which also matches any spec command carrying the bare token `install`; `argv[1] == "install"` keeps the discrimination.
**Decisions**:
Verdict(decision='tasks', tasks=['In src/mcp_coder/install/_phases.py around line 155, restore the explicit skip message in phase_overrides: when the resolved local_path has no pyproject.toml or no install-from-github section, print a line saying GitHub overrides are being skipped and why, matching the behaviour the old tools/install.py had and the pitfall installation.md/README.md document for --source git without --local-path.', 'In tests/install/test_install_phases.py, make _run_install_check hermetic: stub/patch ensure_system_uv (and any other real-environment probe it reaches) so the dry-run tests never shell out to install uv or call sys.exit depending on whether uv is on PATH.', 'In src/mcp_coder/utils/pyproject_config.py near get_install_extras (line ~154), validate the TOML value\'s type before returning it: raise the same kind of explicit configuration error used elsewhere in the strict read when extras is not a string, so a mistyped extras = ["dev"] fails with a clear target-repo message instead of producing the spec mcp-coder[[\'dev\']] or an AttributeError in phase_install_main.', 'In tests/workflows/vscodeclaude/test_session_setup_flow.py around line 98, tighten _is_install back to positional matching (argv[1] == "install") instead of "install" in argv, so it cannot match an unrelated command that merely contains the token.'], escalate_reason=None)
**Changes**:
applied
