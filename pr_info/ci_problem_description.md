The CI mypy job fails with 6 errors across two files, all related to strict type checking.

In `tests/icoder/test_info_command.py` at lines 259 and 287, the `mock_load` parameter in `test_info_shows_prompt_paths` and `test_info_shows_shipped_defaults` is typed as `object` rather than `unittest.mock.MagicMock`. Because `object` has no `return_value` attribute, mypy raises `attr-defined` errors on both lines. The existing `# type: ignore[union-attr]` comments suppress the wrong error code (these are `attr-defined` errors, not `union-attr`), so mypy also flags them as `unused-ignore`. The fix is to change the type annotation of `mock_load` from `object` to `MagicMock` (imported from `unittest.mock`) and remove the now-unnecessary `# type: ignore[union-attr]` comments on those lines.

In `tests/llm/providers/claude/test_claude_cli_stream_parsing.py` at line 12, the test imports `get_stream_log_path` and `sanitize_branch_identifier` from `mcp_coder.llm.providers.claude.claude_code_cli`. These symbols are not defined in that module — they are imported there on line 19 from the submodule `claude_code_cli_log_paths` for internal use, but under mypy strict mode implicit re-exports are disallowed. The fix is either to make the re-export explicit in `claude_code_cli.py` (e.g., by adding `as get_stream_log_path` / `as sanitize_branch_identifier` to the import, or by adding an `__all__` list that includes them), or to update the test to import directly from `mcp_coder.llm.providers.claude.claude_code_cli_log_paths`.

Files requiring changes: `tests/icoder/test_info_command.py` (fix mock parameter types on lines 253 and 281, remove stale type-ignore comments on lines 259 and 287), and either `src/mcp_coder/llm/providers/claude/claude_code_cli.py` (add explicit re-exports) or `tests/llm/providers/claude/test_claude_cli_stream_parsing.py` (change import source to `claude_code_cli_log_paths`).
