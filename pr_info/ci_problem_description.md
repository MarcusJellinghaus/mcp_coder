The CI failure is in `tests/workflows/test_create_pr_integration.py::TestWorkflowMainFunction::test_main_workflow_pr_creation_failure`. The test calls `run_create_pr_workflow(Path("/test/project"))` with mocks for `check_prerequisites`, `generate_pr_summary`, `git_push`, and `create_pull_request`, but it does not mock `cleanup_repository` or `is_working_directory_clean`. After the workflow reorder (issue #759), cleanup now runs as step 3 — between summary generation (step 2) and push (step 4). This means the unmocked `is_working_directory_clean` is called on the fake path `/test/project`, which hits the real `is_git_repository()` check in `src/mcp_coder/utils/git_operations/repository_status.py:31` and raises `ValueError: Directory is not a git repository: /test/project`.

The fix requires adding two additional mocks to `test_main_workflow_pr_creation_failure` in `tests/workflows/test_create_pr_integration.py`: one for `mcp_coder.workflows.create_pr.core.cleanup_repository` (returning `True`) and one for `mcp_coder.workflows.create_pr.core.is_working_directory_clean` (returning `True` to skip the commit path). These mocks are needed because the workflow reorder moved cleanup before push and PR creation, but this test was not updated to account for the new step ordering.

The files involved are `tests/workflows/test_create_pr_integration.py` (the failing test, around lines 386-408) and `src/mcp_coder/workflows/create_pr/core.py` (the reordered workflow, lines 529-549 where cleanup now runs as step 3). The production code is correct per the plan; only the test needs the missing mocks added.
