The CI pipeline has two failing jobs: `pylint` and `file-size`, both caused by changes to `tests/utils/git_operations/test_compact_diffs.py` introduced in the issue #709 implementation (compact-diff header-only changes).

The `pylint` job fails with exit code 4 due to two W1404 (implicit-str-concat) warnings at lines 879 and 884 of `tests/utils/git_operations/test_compact_diffs.py`. This pylint rule flags adjacent string literals inside a function call that are implicitly concatenated by Python (e.g. `("foo" "bar")` instead of `("foo" + "bar")` or `("foobar")`). This typically happens in `pytest.mark.parametrize` argument lists or similar calls where multiline strings are placed next to each other without an explicit comma or concatenation operator. The fix is to either join the strings explicitly with `+`, use a single combined string, or add a missing comma if the strings were meant to be separate list elements.

The `file-size` job fails because the same file grew to 927 lines, exceeding the project's 750-line maximum enforced by `mcp-coder check file-size --max-lines 750 --allowlist-file .large-files-allowlist`. The file is not in the allowlist. The preferred fix is to split the file into smaller modules — for example, extracting the new header-only change type tests into `test_compact_diffs_header_only.py` — rather than adding it to the allowlist.

Both fixes target the same file: `tests/utils/git_operations/test_compact_diffs.py`. The implicit string concatenation issues at lines 879 and 884 should be fixed first (likely inside a parametrize decorator or similar call), and then the file should be split to bring it under the 750-line limit. No production code changes are needed — both failures are test-only.
