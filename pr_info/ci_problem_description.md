The CI `file-size` job failed because `tests/utils/test_user_config.py` has grown to 833 lines, exceeding the project's 750-line maximum enforced by `mcp-coder check file-size --max-lines 750 --allowlist-file .large-files-allowlist`. This file is not in the `.large-files-allowlist`, so the check fails. The file grew beyond the limit because step 1 of the implementation plan added new tests for `find_repo_section_by_url()` to this already-large test file.

The fix should split `tests/utils/test_user_config.py` into smaller, focused test modules. Since the PR added `find_repo_section_by_url()` tests, a natural split would be to extract those new tests (and possibly other logically grouped tests) into a separate file such as `tests/utils/test_user_config_repo_matching.py`. This keeps the original file under the 750-line limit without needing to add it to the allowlist, which would go against the project's goal of reducing large files over time (per issue #353 referenced in the allowlist).

A secondary CI failure in `ruff-docstrings` may also need attention, but it is independent of the file-size issue. The primary files involved in the fix are `tests/utils/test_user_config.py` (to be split) and potentially `tests/utils/conftest.py` if shared fixtures need adjustment.
