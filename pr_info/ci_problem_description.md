The CI `ruff-docstrings` job fails with error DOC502 on `src/mcp_coder/workflow_utils/task_tracker.py` at line 388-400. The `get_task_counts` function documents `TaskTrackerFileNotFoundError` and `TaskTrackerSectionNotFoundError` in its `Raises:` docstring section, but neither exception is explicitly raised with a `raise` statement within the function body. Instead, these exceptions are raised indirectly by the helper functions `_read_task_tracker()` and `_find_implementation_section()` that `get_task_counts` calls. Ruff's DOC502 rule requires that exceptions listed in `Raises:` docstrings correspond to explicit `raise` statements in the function itself, not in called functions.

The fix is in `src/mcp_coder/workflow_utils/task_tracker.py`. The `Raises:` section (lines 397-399) should be removed from the `get_task_counts` docstring since the function does not directly raise those exceptions. This is the same pattern used by other public functions in the file like `get_incomplete_tasks`, `has_incomplete_work`, and `is_task_done`, which also delegate to the same helpers but do not document the indirectly-raised exceptions.

The other failing CI jobs (file-size, mypy, pylint) are separate issues unrelated to this docstring lint failure and should be investigated independently.
