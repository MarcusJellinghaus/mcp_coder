The CI pipeline failed in the pylint job due to an unused variable `mock_counts` at line 1014 in `tests/checks/test_branch_status.py`. Pylint reported warning W0612 (unused-variable), and since the CI is configured to fail on warnings (exit code 4), this caused the job to fail. The mypy job also failed, likely due to related type issues from the same set of changes.

These failures stem from the implementation of issue #676, which replaced the `tasks_complete: bool` field in `BranchStatusReport` with a richer `TaskTrackerStatus` enum, `tasks_reason` string, and `tasks_is_blocking` boolean. The implementation spans three steps: adding the enum/counts to `task_tracker.py`, updating the dataclass and `_collect_task_status()` in `branch_status.py`, and updating formatters/recommendations. During the test updates in `tests/checks/test_branch_status.py`, a variable `mock_counts` was assigned (likely a mock return value for `get_task_counts()`) but never actually used in the test assertion or setup at line 1014.

The fix is straightforward: in `tests/checks/test_branch_status.py` around line 1014, either remove the unused `mock_counts` variable entirely (if the mock is set up differently), prefix it with an underscore to indicate it's intentionally unused, or use it in the test as originally intended. The mypy failure should also be investigated in the same file and in `src/mcp_coder/checks/branch_status.py` for any type annotation mismatches introduced by the `TaskTrackerStatus` migration.
