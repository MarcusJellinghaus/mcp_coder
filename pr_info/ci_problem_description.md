The CI `file-size` job failed because three files exceed the 750-line maximum and are not listed in `.large-files-allowlist`. The violations are: `tests/llm/providers/langchain/test_langchain_provider.py` (943 lines), `tests/llm/providers/langchain/test_langchain_agent.py` (771 lines), and `src/mcp_coder/llm/providers/claude/claude_code_cli.py` (768 lines). These files grew beyond the limit as part of the Issue #648 implementation (system prompt + project prompt support), which added new test cases to the langchain test files and new parameters/logic to `claude_code_cli.py`.

The two test files are the highest priority to fix since they have the largest overages. `test_langchain_provider.py` at 943 lines is nearly 200 lines over the limit and likely contains test groups that can be split into separate test modules (e.g., system-prompt-related tests could move to a dedicated file). `test_langchain_agent.py` at 771 lines is just over the threshold and similarly could have new agent-related prompt tests extracted. The source file `claude_code_cli.py` at 768 lines is only 18 lines over and may be addressable by extracting the new prompt-building logic into a helper module or by minor restructuring.

The preferred fix is to refactor these files to bring them under 750 lines, following the project's refactoring guide at `docs/processes-prompts/refactoring-guide.md`. The test files should be split by extracting cohesive test groups into new test modules within the same directories. For `claude_code_cli.py`, the new `build_cli_command` prompt parameters and related logic could potentially be extracted. As a last resort, files can be added to `.large-files-allowlist`, but the allowlist is intended to be reduced over time (per issue #353), not grown. The other failing CI jobs (mypy, ruff-docstrings) likely have related issues from the same PR changes and should be checked after the file-size violations are resolved.
