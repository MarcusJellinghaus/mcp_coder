The CI `file-size` job failed because `src/mcp_coder/checks/branch_status.py` has grown to 764 lines, exceeding the project's 750-line maximum enforced by `mcp-coder check file-size --max-lines 750 --allowlist-file .large-files-allowlist`. This file is not in the `.large-files-allowlist`, so it triggers a hard failure.

The file grew beyond the limit due to the implementation of issue #676, which added the `CIStatus` enum, expanded the `BranchStatusReport` dataclass with new fields (`tasks_status`, `tasks_reason`, `tasks_is_blocking`), rewrote `_collect_task_status()` to return a richer tuple, and updated the formatters (`format_for_human`, `format_for_llm`) and `_generate_recommendations()` with new N/A/ERROR handling logic. These additions collectively pushed the file 14 lines over the threshold.

The fix requires refactoring `src/mcp_coder/checks/branch_status.py` to bring it under 750 lines. The recommended approach is to extract code into a separate module — for example, moving the formatting functions (`format_for_human`, `format_for_llm`, `_generate_recommendations`, and their helpers) into a new file such as `src/mcp_coder/checks/branch_status_formatting.py`, or extracting the new `CIStatus` enum and `TaskTrackerStatus`-related logic into a constants or types module. The `mcp__tools-py__move_symbol` refactoring tool can handle this safely by updating all imports project-wide. After refactoring, all three CI checks (pylint, mypy, pytest) must also pass, as those jobs are also currently failing.
