The CI pipeline pylint job fails with exit code 2 due to two import errors on `src/mcp_coder/mcp_tools_py.py` line 28: `E0401: Unable to import 'mcp_tools_py.formatter.runner'` and `E0611: No name 'runner' in module 'mcp_tools_py.formatter'`. The `run_format_code` shim added in step 1 of the formatter consolidation (#737) uses a lazy import `from mcp_tools_py.formatter.runner import run_format_code` inside the function body. While the `# type: ignore[import-not-found]` comment suppresses mypy, pylint independently validates imports and cannot resolve the module. This means either the installed `mcp_tools_py` package version in CI does not include the `formatter.runner` submodule, or pylint cannot discover it through static analysis of the installed package.

The mypy job also fails, which is consistent — both tools flag the same unresolvable import path. The code at line 28-30 of `src/mcp_coder/mcp_tools_py.py` is the sole location of the problem. The `run_format_code` function was added as a thin shim that delegates to the `mcp_tools_py` library, following the same pattern as the existing `run_mypy_check` shim in the same file, but the formatter module path it references does not exist in the version of `mcp_tools_py` available to CI.

The fix requires one of two approaches: (1) ensure the CI environment installs a version of `mcp_tools_py` that includes `mcp_tools_py.formatter.runner`, or (2) if the module genuinely doesn't exist yet in the published package, add pylint disable comments (`# pylint: disable=import-error,no-name-in-module`) alongside the existing mypy suppression on lines 28-29 of `src/mcp_coder/mcp_tools_py.py` as a temporary measure until the dependency is updated. The file `src/mcp_coder/mcp_tools_py.py` is the only file that needs changes to resolve both the pylint and mypy CI failures.
