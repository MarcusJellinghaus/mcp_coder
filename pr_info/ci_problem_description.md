The CI mypy job fails with a single error in `src/mcp_coder/cli/commands/check_branch_status.py` at line 29: `Module "mcp_coder.workflows.implement.core" does not explicitly export attribute "check_and_fix_ci" [attr-defined]`. This is a mypy strict-mode error caused by the recent refactoring that extracted CI operations from `core.py` into a new `ci_operations.py` module.

As part of the split (described in the implementation plan), `check_and_fix_ci` was moved to `src/mcp_coder/workflows/implement/ci_operations.py` and then re-imported into `core.py` at line 46 (`from .ci_operations import check_and_fix_ci`). However, `core.py` does not include `check_and_fix_ci` in an `__all__` list, so mypy's strict mode considers it a private import rather than a public re-export. When `check_branch_status.py` imports it via `from ...workflows.implement.core import check_and_fix_ci`, mypy flags the non-explicit export.

The fix requires one of two approaches: (1) add an `__all__` list to `core.py` that includes `"check_and_fix_ci"`, which tells mypy this is an intentional re-export, or (2) change the import in `check_branch_status.py` line 29 to import directly from the canonical location: `from ...workflows.implement.ci_operations import check_and_fix_ci`. Option 2 is cleaner since it imports from the module where the function actually lives, avoids the indirection through `core.py`, and aligns with the refactoring goal of making `ci_operations.py` the authoritative location for CI-related functions.
