Remove structlog exception lines from .importlinter

Now that data_files.py and jenkins_operations/client.py have been
refactored to use standard Python logging with extra={} instead of
structlog, remove the temporary exceptions from the structlog isolation
contract in .importlinter.

The structlog_isolation contract now correctly enforces that only
log_utils.py may import structlog, maintaining library isolation.

Changes:
- Remove exception for mcp_coder.utils.data_files -> structlog
- Remove exception for mcp_coder.utils.jenkins_operations.client -> structlog
- Remove the associated TODO comment referencing issue #275

Verified:
- No structlog imports exist outside log_utils.py
- Pylint passes with no issues
- Mypy passes with no type errors
