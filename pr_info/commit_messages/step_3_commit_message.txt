feat: integrate strip_claude_footers() into commit message generation

Integrate footer stripping functionality into generate_commit_message_with_llm() function to automatically clean Claude Code footers from LLM-generated commit messages:

- Add single line call to strip_claude_footers() after parse_llm_commit_response()
- Integration point placed before validation to ensure clean messages
- Update existing tests to include Claude footers in mock LLM responses
- Modified test_generate_commit_message_success and test_generate_commit_message_with_custom_provider_method
- Mock responses now include both robot emoji and co-authored footers
- Preserve all existing error handling and function signatures
- Maintain backward compatibility with existing commit message flow

End-to-end functionality: LLM response with footers → parse → strip footers → validate → clean commit message. All quality checks (pylint, mypy) pass with no issues related to the integration.