feat: add TypedDict for coordinator cache data structure

Add CacheData TypedDict definition for improved type safety in cache operations:

- Define CacheData TypedDict with last_checked and issues fields
- Update _load_cache_file() return type from Dict[str, Any] to CacheData
- Update _save_cache_file() parameter type from Dict[str, Any] to CacheData
- Add type cast for JSON loaded data to ensure mypy compatibility

This change improves type safety for cache operations while maintaining 
backward compatibility with existing cache file format and functionality.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>