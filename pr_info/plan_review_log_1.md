# review-plan review log 1

## Round 1 — 2026-09-06
**Findings**:
I'll gather context now.`pr_info/steps/step_4.md:84` — high — `_find_section_array` matches *any* depth-1 string equal to the section name, but `defaultMode` is a top-level key whose schema enum is exactly `"allow"|"ask"|"deny"` (`loader.py:146`); `{"defaultMode": "allow", ...}` therefore hits the match, the "expect `:` then `[`" walk fails, and the pseudo-code specifies no continue-scanning fallback — the locator returns `None` and `_insert_section` writes a duplicate top-level `"allow"` key, corrupting the file. No test in the table covers a file containing `defaultMode`.

`pr_info/steps/step_4.md:122` — high — `write_rule` does `json.loads(_strip_jsonc(text))` with no stated failure behaviour; a hand-edited/malformed `settings.local.json` raises `JSONDecodeError` (a `ValueError`) and a non-object root raises `AttributeError` on `data.get(section, [])`. `step_5.md:50` catches only `OSError`, so the exception escapes on the UI thread, `resolve_pending` is never called, and the parked interceptor wedges the turn permanently — the exact failure the step-5 degrade branch exists to prevent.

`pr_info/steps/step_3.md:154` — medium — `test_session_grant_absent_from_a_reloaded_config` is vacuous as specified: `tmp_path` has no `.icoder/`, so `load_permission_config(tmp_path)` returns an empty config and "contains no `runtime`-layer rule" holds whether or not the grant was applied. It needs an authored `.icoder/settings.json` on disk to be a real assertion.

`pr_info/steps/step_3.md:153` — medium — `test_session_grant_is_honoured_by_resolve` hand-builds `PermissionConfig` from the `Rule` captured by the `add_runtime_rule` spy, so the gateway's real runtime-layer store (`gateway.py::add_runtime_rule`, which rebinds the frozen config) is never exercised; the AC "honoured by `resolve()` on a *subsequent* turn this process" is only proved for the rule shape, not the store.

`pr_info/steps/summary.md:5` — medium — step 1 implements issue #1154 in full (its own summary, ACs, tests and commit message `fix(permissions): ... (#1154)`) inside the #1046 PR; #1154 is a separate open issue still at `status-01:created` and has not been through its own plan/review cycle.

`pr_info/steps/step_3.md:110` — low — `_persist_target` falls back to `Path.cwd()` when `runtime_info` is `None`, so a persist grant can write `.icoder/settings.local.json` into a directory unrelated to the project whose config was loaded; skipping the disk write is the safer degrade.

`pr_info/steps/step_2.md:170` — low — acceptance says "All seven tests pass" (repeated at `:187`) but the TDD table at `:159`–`:166` lists six tests.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_4.md around line 84, fix `_find_section_array` so a depth-1 string equal to the section name is only accepted when it is followed by `:` then `[`; on failure the locator must continue scanning for a later match rather than returning `None`. Add a test to the step-4 table covering a settings file containing a top-level `"defaultMode": "allow"` alongside (and without) an `allow` array, asserting no duplicate top-level key is written.', "In pr_info/steps/step_4.md around line 122, specify `write_rule` failure behaviour for malformed input: catch `json.JSONDecodeError`/`ValueError` from `json.loads(_strip_jsonc(text))` and a non-object root, and surface them as the same failure type the caller already degrades on. Update step_5.md:50 so the caller's except clause covers that type, guaranteeing `resolve_pending` is still called and the interceptor cannot wedge the turn. Add a test for a malformed `settings.local.json` and for a non-object root."], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-09-06
**Findings**:
I'll gather context now.`pr_info/steps/step_2.md:87` — high — `_format_args` does not render full args: `stream_renderer._render_value_full` truncates any single-line string >120 chars to `value[:117] + "..."`, so a long one-line command/URL/JSON arg is unreachable in the modal even by scrolling, violating the issue's DECIDED args-visibility rule ("the full args must be reachable"); `pr_info/steps/summary.md:23` states the opposite ("Full, untruncated arg block").

`pr_info/steps/step_2.md:162` — high — `test_approval_modal_args_widget_holds_full_args` asserts `widget.text == _format_args(args)`, which is tautological: with the prescribed >120-char value both sides carry the same `...`, so the test passes while the AC ("no truncation or ellipsis") is violated; it must assert the raw arg value appears verbatim.

`pr_info/steps/step_5.md:49` — high — the disk write runs before `_grant_rule`'s `parse_matcher` guard, so a canonical name `parse_matcher` rejects (e.g. `mcp__srv__do__it` — `_parse_token` requires exactly two `__`-separated parts; reachable whenever `defaultMode: "ask"` sends unmatched tools to the prompt) is written verbatim into `settings.local.json`, fails `_load_layer` on the next launch and degrades the whole config fail-closed; the write must be gated on a successful parse.

`pr_info/steps/step_4.md:143` — high — `write_rule` validates only that the root is a dict, not that each section value is a list: a JSON-valid `{"allow": "mcp__a__b"}` passes both `PersistError` guards, `matcher in data.get(section, [])` degenerates to a substring test, `_find_section_array` then returns `None`, and the `*_find_section_array(...)` unpack at `:145` raises `TypeError` — not an `OSError`, so it escapes step 5's single degrade branch, skips `resolve_pending` and wedges the turn.

`pr_info/steps/step_1.md:34` — high — hoisting `_LAYER_ORDER` above `policy.rank` also reverses the `user`↔`project` pair: a repo-committed `.icoder/settings.json` `"allow"` now silently overrides the user's global `"ask"` at equal specificity. #1046 only requires `local`/`runtime` to beat `project`; the step's fail-closed bound reasons solely about `never`, and none of the six new cases pins the user↔project direction.

`pr_info/steps/step_3.md:154` — medium — `test_session_grant_absent_from_a_reloaded_config` is vacuous: `tmp_path` has no `.icoder/`, so `load_permission_config` returns an empty config and the assertion holds whether or not the grant was applied; it needs an authored `.icoder/settings.json` on disk.

`pr_info/steps/step_3.md:153` — medium — `test_session_grant_is_honoured_by_resolve` hand-builds `PermissionConfig` from the spied `Rule`, so the real store (`gateway.add_runtime_rule`, which rebinds the frozen config) is never exercised; `AppCore` accepts `permission_gateway=`, so a real gateway can be injected instead.

`pr_info/steps/step_3.md:108` — medium — `_persist_target` re-derives `Path(runtime_info.project_dir) else Path.cwd()`, which `ICoderApp.__init__` already computes as `self._project_dir` (`src/mcp_coder/icoder/ui/app.py:87`) — two implementations that can drift, the same objection the plan raises against inlining `action_cancel_stream`.

`pr_info/steps/step_2.md:170` — low — acceptance says "All seven tests pass" (repeated at `:187`) but the table at `:159`–`:166` lists six.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_2.md around line 87, fix the args-visibility rule: `_format_args` must not route long single-line values through `stream_renderer._render_value_full`'s 120-char truncation. Render arg values verbatim (wrapping/scrolling is fine), so the full arg text is reachable in the modal, matching summary.md:23.", 'In pr_info/steps/step_2.md around line 162, make `test_approval_modal_args_widget_holds_full_args` non-tautological: assert the raw >120-char arg value appears verbatim in `widget.text` and that no `...` ellipsis was introduced, rather than comparing against `_format_args(args)`.', 'In pr_info/steps/step_5.md around line 49, gate the disk write on a successful `parse_matcher`: run the same canonical-name parse guard `_grant_rule` uses before writing to `settings.local.json`, and skip the write (degrading as specified) when the matcher does not parse. Add a test with a name like `mcp__srv__do__it` asserting nothing is written and the next-launch config still loads.', 'In pr_info/steps/step_4.md around line 143, extend `write_rule`\'s validation beyond a dict root: if the target section\'s value exists and is not a list, raise `PersistError` rather than falling through to `matcher in data.get(section, [])` and the `*_find_section_array(...)` unpack. Add a test for `{"allow": "mcp__a__b"}` asserting `PersistError` (not `TypeError`) and that step 5\'s degrade branch still calls `resolve_pending`.', 'In pr_info/steps/step_1.md around line 34, preserve the existing `user` vs `project` precedence when hoisting `_LAYER_ORDER` above `policy.rank` — only `local` and `runtime` should gain precedence over `project`, per #1046. Add a test case pinning that a user-layer `ask` still wins over a project-layer `allow` at equal specificity.'], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-09-06
**Findings**:
I'll gather context now.`pr_info/steps/step_4.md:149` — high — the move branch does `_remove_item(text, _find_item(text, _scan(text), *_find_section_array(...), matcher))`: `_find_section_array` is `tuple | None` and `_find_item` is `Span | None`, yet neither result is checked before the `*` unpack / the `_remove_item(text, span: Span)` call. A file with a duplicate top-level key (`{"ask": [], "ask": ["mcp__a__b"]}` — `json.loads` keeps the second, the locator returns the first, empty array) yields `TypeError`, not `PersistError`. That breaks the module's stated "one failure type" contract (`step_4.md:180-185`) that step 5's single `except OSError` (`step_5.md:57`) relies on, so the exception escapes onto the UI thread, `resolve_pending` is skipped and the turn wedges — the exact failure round 2's non-list-section guard was added to prevent, left open on the adjacent path.

`pr_info/steps/step_4.md:126` — medium — `comma = "" if the root object body holds no code characters else ","` never says where the root object body ends, and `_insert_section` locates only the opening `{`. On the new-file skeleton `"{\n}\n"` the closing `}` is itself a code character, so a literal reading writes a trailing comma into a brand-new `settings.local.json`. No test in the table can distinguish it: `test_creates_file_and_directory_when_absent` and `test_absent_key_gets_a_scaffold` both parse through `_strip_jsonc`, which strips trailing commas.

`pr_info/steps/step_1.md:51` — low — "exactly two behaviour changes … and a `never` is no longer beaten by a higher layer" is inaccurate: under the current key `Policy.rank` already sits above `_LAYER_ORDER`, so a `never` already wins at equal specificity. `never_bit` only neutralises the newly hoisted `personal_bit`, and `test_user_never_beats_local_allow_at_equal_specificity` (`step_1.md:108`) therefore passes both before and after the change.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_4.md around line 149, guard the move branch\'s `None` results: check `_find_section_array(...)` before the `*` unpack and `_find_item(...)` before calling `_remove_item`, raising `PersistError` when either is `None` so the module keeps its single-failure-type contract (step_4.md:180-185) that step 5\'s `except OSError` relies on. Add a test with a duplicate top-level key (e.g. `{"ask": [], "ask": ["mcp__a__b"]}`) asserting `PersistError` rather than `TypeError`, and that `resolve_pending` is still called.'], escalate_reason=None)
**Changes**:
applied
