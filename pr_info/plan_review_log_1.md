# review-plan review log 1

## Round 1 — 2026-09-06
**Findings**:
I'll gather context first.Now verifying plan claims against the actual code.`pr_info/steps/step_5.md:87` — high — CI bootstrap `uv pip install --system .` resolves `mcp-workspace` / `mcp-coder-utils` from PyPI; every other job in `ci.yml:123-124` installs the `[tool.mcp-coder.install-from-github]` specs first, and mcp-tools-py's `ci.yml:115-116` records that the PyPI copies are "too old for mcp-coder's imports" — the bootstrapped `mcp-coder install` can fail on `import mcp_coder` before the job's own install ever runs.
`pr_info/steps/step_6.md:45` — high — the replacement README recipe `mcp-coder install ~/mcp-coder-env --source git` targets a directory with no `pyproject.toml`, so under Decisions 10+17 the GitHub sibling overrides are silently skipped; the retired curl recipe fetched `pyproject.toml` precisely to apply them, and §2 removed the check that surfaced the resulting env. Same defect in the `installation.md` rewrite at `step_6.md:26`.
`pr_info/steps/step_4.md:45` — high — ALGORITHM calls the private `_load_pyproject` from `pyproject_config` across packages, contradicting HOW (`step_4.md:33`, "reads extras via `get_install_extras`") and the reasoning behind Decision 16; `get_install_extras` returns `"dev"` for both "declared dev" and "absent", so the extras warn row is not implementable through it — the plan needs a public predicate.
`pr_info/steps/step_2.md:135` — medium — Decision 13's strict load only fires when `--extras` is absent, and `phase_overrides` (`step_2.md:101`) reads the same file via lax `get_github_install_config`; with an explicit `--extras`, a malformed target `pyproject.toml` silently yields no overrides instead of the promised error naming the file.
`pr_info/steps/step_1.md:26` — medium — `format_toml_error` also depends on module-level `_SMART_QUOTES` (`user_config.py:34`, its only user); "hoist `import re` — the only edit to the body" omits moving the constant, leaving either a `NameError` or dead code in `user_config`.
`pr_info/steps/step_5.md:13` — medium — the plan never adds `[tool.mcp-coder.install] extras = "dev"` to this repo's own `pyproject.toml`, although issue §3 places the declaration in the target repo and mcp_coder is the target for both the CI job and `reinstall_local`; the new `validate_target_repo` row therefore warns on the repo that introduces it.
`pr_info/steps/step_3.md:52` — medium — after this commit sessions invoke the coordinator venv's `mcp-coder install`; a coordinator env installed before this PR fails with `invalid choice: 'install'` and no fallback exists, but no step records the coordinator-upgrade prerequisite.
**Decisions**:
Verdict(decision='tasks', tasks=['step_5.md:87 — Fix the CI bootstrap so `mcp-coder install` is runnable: install the `[tool.mcp-coder.install-from-github]` specs (mcp-workspace, mcp-coder-utils) before/alongside `uv pip install --system .`, matching the other jobs in ci.yml:123-124, so bootstrapping does not resolve stale PyPI copies.', 'step_6.md:45 and step_6.md:26 — Correct the README and installation.md recipes: the documented `mcp-coder install <dir> --source git` target has no pyproject.toml, so GitHub sibling overrides (Decisions 10+17) are skipped. Either document a recipe that supplies the target pyproject.toml, or state explicitly how the overrides are applied without it.', 'step_4.md:45 — Resolve the ALGORITHM/HOW contradiction: do not call the private `_load_pyproject` across packages. Specify a public predicate in `pyproject_config` that distinguishes \'extras declared\' from \'extras absent\' (since `get_install_extras` returns "dev" for both) and have `validate_target_repo` use it.', 'step_2.md:135 — Make the strict pyproject load unconditional rather than only when `--extras` is absent, so a malformed target pyproject.toml raises the promised error naming the file even when `--extras` is passed explicitly, instead of `phase_overrides` silently yielding no overrides via the lax `get_github_install_config`.', 'step_1.md:26 — Extend the `format_toml_error` move to also relocate the module-level `_SMART_QUOTES` constant (user_config.py:34) along with the `import re` hoist, so neither a NameError nor dead code is left behind in `user_config`.', 'step_5.md:13 — Add `[tool.mcp-coder.install] extras = "dev"` to this repo\'s own pyproject.toml, so the new `validate_target_repo` row does not warn on mcp_coder itself (the target for both the CI job and `reinstall_local`).'], escalate_reason=None)
**Changes**:
applied
