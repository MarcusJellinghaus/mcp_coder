# review-plan review log 1

## Round 1 — 2026-07-27
**Findings**:
I'll gather context from all three sources in parallel.Let me read the rest of issue #1042 and the linked issues, plus the knowledge base and plan files.Now let me read the plan files and remaining knowledge base files.Now the remaining plan steps and knowledge base files.Let me verify the plan's assumptions against the actual I2.1 code (field names, signatures) since the steps depend heavily on them.The I2.1 code exists. Let me read the model, matcher, resolver, and the existing `__init__.py` to verify the plan's field-name and signature assumptions.Now let me verify the critical `parse_matcher` signature and return shape, which Step 5's whole design depends on.Let me verify two things the steps depend on: the `.importlinter` contract format/naming, and the actual import path of `get_user_app_data_dir`.The `get_user_app_data_dir` function wasn't found by direct definition search. Let me verify it exists and its import path, since Step 4 depends on it.Let me get precise line numbers for the two findings I've identified.`pr_info/steps/step_4.md:30` — medium — Discovery builds project/local paths as `project_dir / ".icoder" / ...` without resolving to absolute; step_4.md:37 and step_5.md then store this directly as `Rule.source_path`, but issue #1042's Decisions ("each rule carries its resolved **absolute** source `Path`") and Loop-A refinement ("loader sets `Rule.source_path` (absolute)") require absolute paths — relative `project_dir` yields relative provenance, which I3.3 write-back depends on. No step calls `.resolve()` and no test asserts absoluteness.
`pr_info/steps/step_6.md:65` — low — Test description says `resolve("mcp__x__y", None, None, config)` "returns `Policy.AFTER_APPROVAL`" (and step_6.md:69 "returns `Policy.ALWAYS`"), but `resolve()` returns a `Decision`, not a `Policy`; the assertion must read `.policy`. Inaccurate API in the planned assertion.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_4.md (discovery, ~line 30) and step_5.md (~line 37), resolve the project/local paths to absolute before storing as Rule.source_path — call .resolve() on the constructed `project_dir / ".icoder" / ...` paths so provenance is absolute as required by issue #1042 Decisions and Loop-A refinement, and add a test asserting Rule.source_path.is_absolute().', 'In pr_info/steps/step_6.md (~lines 65 and 69), correct the planned test assertions: resolve() returns a Decision, not a Policy, so assert on `.policy` (e.g. resolve(...).policy == Policy.AFTER_APPROVAL / Policy.ALWAYS).'], escalate_reason=None)
**Changes**:
applied
