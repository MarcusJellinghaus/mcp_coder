# review-plan review log 1

## Round 1 — 2026-07-27
**Findings**:
I'll gather context systematically. Let me start by exploring the knowledge base, the issue, and the plan files.The issue is very detailed. Now let me read the knowledge base files and the plan files.Now let me read the plan files.Let me verify key claims against the actual codebase — the `__init__.py` structure and the version fallback pattern.Let me examine the pyproject.toml dependency structure to validate the marker-evaluation and extras claims.Let me verify the pytest configuration claim (`pythonpath=["src"]`, metadata-absent) and check the `[tool.pytest]` and editable-install situation.This is important to verify: does `packaging`'s `Marker.evaluate()` with the default environment (no `extra` key) correctly return `False` for `extra == '...'` markers, or does it raise? If it raises, the guard would fail-open on every real install and never fire. Let me check the packaging source.`packaging`'s current behavior defaults `extra` to `""` under `context="metadata"`, so `evaluate()` returns `False` correctly — but only in newer `packaging`. Older versions (< ~24.1, before the `context` parameter) raise on an unset `extra`, which would make `find_missing_dependencies()` raise and the guard fail-open on every real install. Let me get exact line numbers to anchor findings.Line 46–49 and 64 specify `req.marker.evaluate()` with no explicit environment. Let me anchor the test-case-4 line too.I have sufficient context. The plan is well-aligned with the issue, internally consistent, KISS, and structured as two testable one-commit TDD steps. All seven issue requirements are covered, sequencing is correct (Step 1 creates the standalone module + tests; Step 2 wires the fail-open guard), and no false-positive path exists on healthy installs (dist-name matching and platform markers are handled correctly). One substantive robustness gap:

pr_info/steps/step_1.md:64 — medium — `req.marker.evaluate()` is called with no explicit environment; correct `extra == "..."` → False behavior depends on `packaging` defaulting `extra` to `""` under `context="metadata"`, which only exists in newer `packaging` (~24.1+). On older `packaging`, evaluating an `extra` marker raises `UndefinedEnvironmentName`/`KeyError`, which propagates out of `find_missing_dependencies()` and makes the `__init__` guard fail-open on EVERY real install (mcp-coder always has extras in `requires()`), silently defeating the feature. `packaging` is undeclared/unpinned (summary.md), so the version is not guaranteed. Harden by passing an explicit environment (e.g. `req.marker.evaluate(environment={"extra": ""})`) and/or wrapping the per-requirement marker check in `try/except` so one odd spec cannot disable the whole guard.

pr_info/steps/step_1.md:100 — low — test case 4 (`["some-pkg; extra == 'dev'"]` → `[]`) asserts the same version-dependent marker behavior; on older `packaging` this test errors rather than passing, so it will not catch the regression above and pins the plan to newer-`packaging` runners without stating a minimum.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_1.md, harden the marker evaluation in find_missing_dependencies() so an extra-marker (e.g. `extra == 'dev'`) reliably evaluates to False across packaging versions: pass an explicit environment such as `req.marker.evaluate(environment={'extra': ''})`, and wrap the per-requirement marker check in try/except so one unparseable/odd spec cannot make the whole guard raise and fail-open on real installs.", 'In pr_info/steps/step_1.md, update test case 4 (`["some-pkg; extra == \'dev\'"]` → `[]`) to assert the version-independent behavior guaranteed by the hardened marker evaluation, and either declare a minimum `packaging` version in summary.md or make the test robust to older `packaging` so it actually catches the fail-open regression.'], escalate_reason=None)
**Changes**:
applied
