# review-plan review log 1

## Round 1 — 2026-08-04
**Findings**:
Let me get precise line numbers for the findings.`pr_info/steps/step_2.md:41` — medium — Cancellation D2/#3 part (c) is not demonstrated: the AC requires proving "the generic paths function as a backstop afterwards" (once the Future resolves), but Step 2's only scenarios are `scenario_inert` (blocked) and `scenario_direct` (direct resolve + thread death). No scenario re-arms `cancel_event`/`GeneratorExit` post-resolution to show the backstop actually fires; it is only asserted as prose in Step 6 FINDINGS §4.
`pr_info/steps/step_5.md:57` — medium — Deny path hardcodes `ToolMessage(..., tool_call_id="", ...)`. An empty `tool_call_id` will not match the pending tool call in the AI message, which can make LangChain reject the message or wedge the turn — directly undermining `scenario_deny`'s "agent continues" (#5) assertion. The id must come from `request` (the real `build_deny_tool_message` derives it), not `""`.
`pr_info/steps/step_4.md:63` — low — Gotcha #2 (side-channel pending-approval event) has no dedicated demonstration despite being an in-scope Loop-A DoD mechanic. Reachability from inside the interceptor is only structurally implied by opaque `object()` `q` sentinels here, and the emit/replay consequence is recorded as FINDINGS prose (Step 6 §6); no step emits an `approval_request` event through a real `q` from the interceptor.
**Decisions**:
Verdict(decision='tasks', tasks=['Fix the deny path at pr_info/steps/step_5.md:57: derive tool_call_id from the request (as the real build_deny_tool_message does) instead of hardcoding tool_call_id="", so the ToolMessage matches the pending tool call in the AI message and scenario_deny\'s \'agent continues\' (#5) assertion actually holds.', 'Add a Step 2 scenario that demonstrates Cancellation D2/#3 part (c): after the Future resolves, re-arm cancel_event/GeneratorExit and show the generic paths fire as a backstop, rather than only asserting it as prose in Step 6 FINDINGS §4 (pr_info/steps/step_2.md:41).', 'Add a step that emits a real approval_request event through an actual q from inside the interceptor to demonstrate Gotcha #2 (side-channel pending-approval event) end-to-end, instead of relying on opaque object() sentinels plus Step 6 §6 prose (pr_info/steps/step_4.md:63).'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-04
**Findings**:
`pr_info/steps/step_5.md:69` — high — Tier C records `gate.loop_id = id(get_running_loop())` (:53-55) but `scenario_resume` asserts only `gate.fired` and event presence; it never asserts the D6 loop-identity comparison. The spike's central go/no-go — interceptor runs on the **agent** loop and **not** the MCPManager daemon loop, *on the real adapter path* — is captured but never checked. Tier A cannot supply this (it has only one loop), so Tier C is the only place the two-loop claim can be proven.

`pr_info/steps/step_5.md:53` — high — The plan never captures the MCPManager daemon-loop id (available via `mcp_manager.py:52-59`) nor an independent agent-loop reference in Tier C, so even if an identity assertion were added there is nothing to compare `gate.loop_id` against. The go/no-go needs `interceptor_loop == agent_loop != daemon_loop`.

`pr_info/steps/step_6.md:16` — high — Step 6 §1 predicates a **positive** verdict on "Tier C proved the interceptor coroutine runs on the agent loop (D6 identity)", but Step 5 produces no such identity proof (see above). As written the spike would emit a positive go/no-go from "fired + resumed" alone — precisely the "the resolve worked only proves the guess was right" trap D6 exists to prevent — handing I3.2 a false "proven".

`pr_info/steps/step_2.md:61` — medium — `scenario_inert` exercises only two of the three named generic cancel paths (`cancel_event`, `GeneratorExit`); the TUI `_cancel_event` (`ui/app.py:290`, a consumer layer above the reconstructed bridge) is neither modelled nor asserted, covered only implicitly by "no events arrived". The AC enumerates all three explicitly; the step should model the third path or state that "no events arrived" is the proof it cannot fire.

`pr_info/steps/step_5.md:42` — low — Tier C answers the central go/no-go yet runs 5th, after Tiers A/B and the D7 seam are built on the unproven assumption that the in-band `await` is viable. Spike best practice de-risks the riskiest unknown first; a negative Tier C would moot Steps 2–4's pause/cancel semantics (low because scripts are throwaway and independent, so rework cost is small).
**Decisions**:
Verdict(decision='tasks', tasks=['In Step 5 Tier C scenario_resume (pr_info/steps/step_5.md:69), add the D6 loop-identity assertion: assert that gate.loop_id (interceptor loop) equals the agent loop, rather than asserting only gate.fired and event presence.', 'In Step 5 Tier C (pr_info/steps/step_5.md:53), capture the MCPManager daemon-loop id (via mcp_manager.py:52-59) and an independent agent-loop reference so the identity check can prove interceptor_loop == agent_loop != daemon_loop.', "In Step 6 §1 (pr_info/steps/step_6.md:16), make the positive go/no-go verdict conditional on the actual D6 identity proof from Tier C, so a positive verdict cannot be emitted from 'fired + resumed' alone.", "In Step 2 scenario_inert (pr_info/steps/step_2.md:61), model or assert the third generic cancel path (TUI _cancel_event at ui/app.py:290) that the AC enumerates — either exercise it explicitly or state that 'no events arrived' is the proof it cannot fire."], escalate_reason=None)
**Changes**:
applied
