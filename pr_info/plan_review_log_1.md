# review-plan review log 1

## Round 1 — 2026-08-30
**Findings**:
I'll gather context now.`pr_info/steps/summary.md:164` — high — Step order puts the only upstream-blocked step first; step_1's precondition says "stop and report", which strands the independent, unblocked step 2. Verified live today: installed `mcp_workspace.checks.branch_status_rendering` has no `LinkedBranchStatus`/`linked_branch_blocks`. Swap the two steps here and in `pr_info/TASK_TRACKER.md:24-25` so the unblocked skill-wording commit can land.
`pr_info/steps/step_1.md:114` — medium — The tier interaction the design rationale rests on is untested: `MISMATCH` + `pr_feedback_undeterminable=True` + `fail_on_reviews=True` must be 2, not 1. The 6x2 table fixes `CIStatus.PASSED` with no review feedback, so hoisting the `linked_branch_blocks` term above the gated tier-2 review term stays green.
`pr_info/steps/summary.md:152` — medium — `tests/checks/test_branch_status.py` (`test_shim_reexports_expected_names`) is the shim's contract test and is not extended; `linked_branch_blocks` is never imported from the shim by any test, so 1b's re-export surface and `__all__` addition are only half-covered.
`pr_info/steps/step_2.md:18` — medium — "upstream suppresses that recommendation on every blocker — ... PR feedback" holds only for `pr_feedback_blocks_merge`; `_generate_recommendations` ignores `pr_feedback_undeterminable`, so an undeterminable review state still emits `Ready to merge`. Record it as a known gap instead of claiming full coverage.
`pr_info/steps/step_2.md:34` — medium — Substring trap in the new criterion: when `pr_mergeable_state` blocks, recommendations contain `Not ready to merge (GitHub mergeable_state: ...)`, which a case-insensitive or naive substring check for `Ready to merge` matches. The step should state the check is case-sensitive / must match the standalone bullet.
`pr_info/steps/step_2.md:20` — low — `Recommendations:` is a section header followed by `- ` bullets (`format_report_for_human`), not a single "`Recommendations` line"; the skill wording being authored should say the section lists a `Ready to merge` bullet.
`pr_info/steps/step_1.md:252` — low — Only exit-code rows 1 and 2 are amended; row 0 still reads "CI passed or still pending ... Success", but a pending-CI run with a blocking linked branch now exits 1, leaving row 0 describing a superset.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/summary.md (around line 164) and pr_info/TASK_TRACKER.md lines 24-25, swap the ordering of step 1 and step 2 so the unblocked skill-wording step comes first; step_1 (which depends on the not-yet-available upstream `LinkedBranchStatus`/`linked_branch_blocks` in mcp_workspace.checks.branch_status_rendering) must not be first with a 'stop and report' precondition that strands the independent step.", 'In pr_info/steps/step_1.md around line 114, add an explicit test case for the tier interaction the design rationale depends on: MISMATCH + pr_feedback_undeterminable=True + fail_on_reviews=True must yield exit code 2, not 1. The existing 6x2 table fixes CIStatus.PASSED with no review feedback and therefore does not exercise hoisting the `linked_branch_blocks` term above the gated tier-2 review term.', 'In pr_info/steps/summary.md around line 152, extend the shim contract test `test_shim_reexports_expected_names` in tests/checks/test_branch_status.py to cover `linked_branch_blocks`, so the 1b re-export surface and the `__all__` addition are actually verified through the shim.', "In pr_info/steps/step_2.md around line 18, correct the claim that upstream suppresses the 'Ready to merge' recommendation on every blocker: `_generate_recommendations` only handles `pr_feedback_blocks_merge` and ignores `pr_feedback_undeterminable`, so an undeterminable review state still emits 'Ready to merge'. Record this as a known gap rather than claiming full coverage.", "In pr_info/steps/step_2.md around line 34, tighten the new acceptance criterion to avoid the substring trap: when `pr_mergeable_state` blocks, recommendations include 'Not ready to merge (GitHub mergeable_state: ...)', which a naive/case-insensitive substring search for 'Ready to merge' would match. State that the check must be case-sensitive and match the standalone 'Ready to merge' bullet."], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-30
**Findings**:
I'll gather context now.`pr_info/steps/step_2.md:313` — medium — 2d amends only exit-code rows 1 and 2; row 0 (`docs/cli-reference.md:692`) still reads "CI passed or still pending, or graceful scenarios ... Success", which after this change is a superset — a PASSED/PENDING-CI run with `MISMATCH`/`AMBIGUOUS`/`NOT_LINKED` now exits 1. Carried from round 1, still unaddressed.
`pr_info/steps/step_2.md:192` — low — The stated red-phase outcome is unobservable: 2a imports `LinkedBranchStatus` from `mcp_coder.checks.branch_status` (`step_2.md:54`), so the whole exit-code test module errors at collection until 2b lands; "the `OK` and `NOT_CHECKED` rows already pass, as does the tier-interaction case's gated half" cannot be seen.
**Decisions**:
Verdict(decision='dismiss', tasks=[], escalate_reason=None)
**Changes**:
dismiss
