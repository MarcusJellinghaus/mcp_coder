# review-plan review log 1

## Round 1 — 2026-08-19
**Findings**:
I'll gather context now.`pr_info/steps/step_2.md:116` — high — The graph-test assertions (`status=="error"`, `tool_call_id=="call_1"`, `invoke_count==2`, last content `"done"`) are all satisfied by a ToolNode-generated tool-error `ToolMessage` (invalid-args or unknown-tool-name templates set `status="error"` + the real `tool_call_id` and the graph still loops back), so the test passes even if the gateway interceptor is never reached — the exact failure mode step_2.md:91 itself warns about ("a schema mismatch would silently skip the interceptor"); assert the deny content (`_DENY_NEVER`) or capture the interceptor request so the test proves the gateway path.

`pr_info/steps/step_2.md:44` — medium — The scripted-model sketch drops the issue's explicit mypy-strict guidance; `_generate`/`_agenerate` as written are untyped defs and will fail the `run_mypy_check` the step lists as a required check (the proven template `spikes/i3-1-approval/_common.py:91` carries `# type: ignore[no-untyped-def]`), leaving the implementer to rediscover it.

`pr_info/steps/step_2.md:9` — medium — Step 2's stated exit criterion is a manual revert of `gateway.py` + restore, which is unautomatable and risks committing the reverted line; the step is also tiny (one test + one CI line) and tightly coupled to step 1 — merging it into step 1 would make the graph test genuinely red-first and remove the manual dance (KB planning_principles: "merge tiny or intertwined steps", "no manual tests").

`pr_info/steps/summary.md:142` — low — "No change to `run_agent_stream`'s stream events" is inaccurate: `src/mcp_coder/llm/providers/langchain/agent.py:613` reads `getattr(output, "tool_call_id", None) or run_id`, so after the fix a denied call's `tool_result` event and the reconstructed history (`agent.py:667-681`) carry the model's id instead of `run_id`; the change is benign (no test asserts the old value) but the plan denies rather than owns it.

`pr_info/steps/step_1.md:69` — low — The outer `getattr(request, "runtime", None)` guards a shape the adapter never produces (`MCPToolCallRequest` is a dataclass that always defines `runtime`, defaulting to `None`), and the new `test_interceptor_deny_without_runtime_uses_empty_id` exists only to cover that speculative branch; `getattr(request.runtime, "tool_call_id", None) or ""` covers every real case, and the step_1.md:60 comment "absent on non-graph/old adapters" is wrong for the non-graph case.
**Decisions**:
Verdict(decision='tasks', tasks=["Strengthen the graph test in pr_info/steps/step_2.md:116 so it proves the gateway interceptor path was exercised: assert the denied ToolMessage content equals the deny message (_DENY_NEVER) and/or capture the interceptor request, instead of relying only on status=='error', tool_call_id=='call_1', invoke_count==2 and last content 'done', which a ToolNode-generated tool-error ToolMessage would also satisfy.", "Add the mypy-strict guidance to the scripted-model sketch in pr_info/steps/step_2.md:44: annotate _generate/_agenerate or carry '# type: ignore[no-untyped-def]' as in spikes/i3-1-approval/_common.py:91, so the step's required run_mypy_check passes without the implementer rediscovering it.", "Merge step_2.md into step_1.md so the graph test is genuinely red-first: remove the manual 'revert gateway.py then restore' exit criterion (unautomatable, risks committing the reverted line) and fold the single test plus CI line into step 1, per KB planning_principles ('merge tiny or intertwined steps', 'no manual tests')."], escalate_reason=None)
**Changes**:
applied
