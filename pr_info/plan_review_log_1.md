# review-plan review log 1

## Round 1 — 2026-08-25
**Findings**:
I'll gather context before reviewing. Starting with the knowledge base, the issue, and the plan files.Now let me verify the plan's claims against the actual source.`pr_info/steps/step_4.md:120` — high — No automated test asserts the issue's primary acceptance criterion end-to-end (`--project-dir X` with no `--execution-dir` ⇒ subprocess `cwd == X`); Step 4 only updates mocked `resolve_execution_dir` call-argument assertions plus a `commit.py` resolver test. `tests/integration/test_execution_dir_integration.py:283-330` already provides the exact pattern (`assert options.cwd == ...`), and the plan touches that test only to rename it — add the positive case there.
`pr_info/steps/step_3.md:50` — medium — Wiring `report_context_root` inside `resolve_execution_dir` makes every call site emit OUTPUT-level lines and perform a root-ward filesystem walk, but the step contains no expected-impact analysis on existing tests, unlike the careful enumeration Step 2 does at `step_2.md:100-111`. Unpatched resolver reachers (`prompt.py`, `commit.py`, `icoder.py` and their command tests) are unassessed.
`pr_info/steps/step_3.md:116` — medium — Test 6 ("nothing anywhere → `[]`") is self-contradictory: the walk runs to the filesystem root, so the prescribed `== []` assertion is exactly the environment-dependent assertion the same sentence warns against. No boundary/injection mechanism is specified to make it deterministic.
`pr_info/steps/step_5.md:85` — medium — Same defect as above for verify test 4 ("none found when no CLAUDE.md exists anywhere up the chain"): unreliable without a stop-boundary, since `tmp_path` ancestors are outside the test's control.
`pr_info/steps/summary.md:286` — medium — `tach.toml`'s `mcp_coder.cli` `depends_on` block does not list `mcp_coder.prompts`, so the new `cli/utils.py → prompts.prompt_loader` import risks breaking `tach check`; no step schedules a `tach.toml` update. The stated justification ("`verify.py` already imports `prompt_loader`") concerns import direction, not the tach allowlist, and tach could not be executed here to confirm.
`pr_info/steps/step_5.md:51` — medium — The "hit lies outside `project_dir`" predicate is written twice and independently (here and `step_3.md:82`), so the run-time report and the `verify` report can drift on the one rule the issue calls load-bearing. Share one predicate.
`pr_info/steps/step_4.md:17` — low — `check_branch_status.py:331` calls `resolve_execution_dir` only inside the `if args.fix > 0:` block, so requirement 5's "once per run" report is absent for `check branch-status` without `--fix`. Step 4's table lists it as an ordinary one-line site without noting the conditional.
`pr_info/steps/summary.md:270` — low — Open decision, answered: keep the `claude_md_paths` variant. It satisfies the constraint's stated purpose with a smaller diff; its only cost is that the ancestor-walk termination logic now exists in both `is_claude_md` and `find_context_claude_md`. Not worth reworking Step 1 to match the issue literally.
`pr_info/steps/step_5.md:19` — low — `tests/cli/commands/test_verify.py` is already 727 lines; adding six tests crosses the repo's documented 750-line guidance. A sibling test module would avoid it.
**Decisions**:
Verdict(decision='tasks', tasks=['Add a positive end-to-end test in tests/integration/test_execution_dir_integration.py (extend Step 4) asserting that when --project-dir X is passed with no --execution-dir, the Claude subprocess options.cwd == X, following the existing pattern at lines 283-330; do not limit Step 4 to renaming that test and mocked resolve_execution_dir call-argument assertions.', 'In step_3.md:50, add an expected-impact analysis for wiring report_context_root inside resolve_execution_dir (OUTPUT-level lines plus root-ward filesystem walk on every call), enumerating affected existing tests for the unpatched resolver call sites prompt.py, commit.py and icoder.py and their command tests, in the same style as step_2.md:100-111.', "Specify a deterministic stop boundary (e.g. an injectable/overridable walk-termination root) for the ancestor walk, and rewrite both step_3.md:116 test 6 ('nothing anywhere -> []') and step_5.md:85 verify test 4 ('none found up the chain') to use it, so the == [] assertion no longer depends on directories above tmp_path.", 'Add a step (or extend an existing one) that updates tach.toml to include mcp_coder.prompts in the mcp_coder.cli depends_on list for the new cli/utils.py -> prompts.prompt_loader import, and run tach check to confirm.', "Factor the 'hit lies outside project_dir' predicate into a single shared helper used by both the run-time report (step_3.md:82) and the verify report (step_5.md:51), instead of specifying it independently in two places.", "In step_4.md:17, note that check_branch_status.py:331 calls resolve_execution_dir only inside the `if args.fix > 0:` block, and specify how requirement 5's once-per-run context report is emitted for `check branch-status` without --fix.", "Place the six new verify tests from step_5.md:19 in a new sibling test module rather than tests/cli/commands/test_verify.py, which is already 727 lines and would exceed the repo's 750-line guidance."], escalate_reason=None)
**Changes**:
applied
