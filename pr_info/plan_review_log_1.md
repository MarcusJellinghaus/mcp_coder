# review-plan review log 1

## Round 1 — 2026-08-23
**Findings**:
I'll gather context now.`pr_info/steps/step_6.md:42` — critical — import cycle: step 5 adds `__init__.py` → `_config_diagnostics.validate` while step 6 has `_config_diagnostics.resolve_target` call `_create_chat_model` from the package `__init__`; with module-level imports this breaks `import mcp_coder.llm.providers.langchain` outright (needs an explicit function-level import, as `verification.py` does for `_models`), and neither step mentions it.
`pr_info/steps/step_8.md:37` — high — "reuse the single `resolve_target()` call already made for the effective-config echo" is unimplementable as planned: step 7 builds the echo in `verify.py` via `describe_effective_config()` (which calls `resolve_target` internally), while `_check_base_url_shape` is called inside `verify_langchain` (`verification.py:260`); no signature or plumbing exists to share the `ResolvedTarget`, so the chat model is constructed twice per run.
`pr_info/steps/step_9.md:59` — high — `result.setdefault(k, ...)` cannot surface the `backend` finding: `result["backend"]` is already populated at `verification.py:199` with `ok=(backend is not None)`, so an unsupported backend renders `Backend [OK] <name>` while `overall_ok` is False — exit 1 with the cause invisible, contradicting the step's own "report every violation" goal and the AC "naming the contract violation".
`pr_info/steps/step_7.md:37` — high — the acceptance criterion "`verify` flags when `OPENAI_API_KEY` overrides a configured `api_key`" is implemented by no step; `_resolve_api_key` (`verification.py:56`) returns only the winning source and never signals that a config `api_key` existed and lost.
`pr_info/steps/step_9.md:35` — medium — deleting the hand-rolled ollama `api_key` case loses its value text: with no finding produced, the row falls back to `_mask_api_key(None)` → `None`, which `_format_section` stringifies to the literal `"None"` instead of `"not set (optional)"`; TDD case 5 asserts only `ok=True`, so the regression passes.
`pr_info/steps/step_8.md:32` — medium — the call-site gate is left unspecified: the shape check runs today only under `if backend == "openai"` (`verification.py:260`). Keeping it leaves the `OLLAMA_HOST` redirect unchecked; broadening it makes the `/v1` heuristic warn on correct ollama hosts (`http://localhost:11434`).
`pr_info/steps/step_7.md:34` — medium — step 7 never states where `verify.py` gets the langchain `config` and `key_source`; both `_load_langchain_config` and `_resolve_api_key` are private llm-layer helpers, and this adds a third `_load_langchain_config()` call per verify run.
`pr_info/steps/step_15.md:19` — medium — `get_system_prompt_path()` is added but consumed by no step (step 16 uses `is_prompt_configured_but_missing`) → dead public API; the repo runs vulture.
`pr_info/steps/step_15.md:45` — medium — module-level `_warned_paths` persists for the whole pytest process; TDD cases 2–4 assert warning counts with no reset fixture, so they are order-dependent under `-n auto`.
`pr_info/steps/step_1.md:5` — medium — "atomic — it cannot be split" contradicts the issue's own outline (the `endpoint_shape` key/label rename is explicitly separable) and the "split large steps" planning principle; ~250 matches over 10 src + 22 test files in a single commit is the largest context risk in the plan.
`pr_info/steps/summary.md:199` — low — "Everything else is independent" is inaccurate: 2 before 11 (`suggest()`), 5 before 6 (shared module + `validate` in `_create_chat_model`), 6 before 10 (`dialed_url`), 15 before 16 (`is_prompt_configured_but_missing`).
`pr_info/steps/step_5.md:33` — low — `_API_KEY_ENV` duplicates `verification._BACKEND_ENV_VARS`; the comment says "reuse" but the plan declares a second table (DRY).
`pr_info/steps/step_5.md:22` — low — the `openai` row's `"api_version": "optional"` cell is unreachable: `mode_of()` routes any config with `api_version` set to `azure`.
**Decisions**:
Verdict(decision='tasks', tasks=['step_6.md:42 — Specify that `_config_diagnostics.resolve_target` must import `_create_chat_model` via a function-level (deferred) import, mirroring how `verification.py` imports `_models`, to avoid the import cycle with the package `__init__` introduced in step 5; note the constraint in both step 5 and step 6.', "step_8.md:37 — Replace the unimplementable 'reuse the single `resolve_target()` call' plan with a concrete design: either pass the already-resolved `ResolvedTarget` into `_check_base_url_shape` (and thread it from `verify_langchain`), or explicitly accept a second resolution; state the chosen signature/plumbing so the chat model is not silently constructed twice per run.", 'step_9.md:59 — Change the plan so the unsupported-backend finding actually surfaces: `result["backend"]` is already populated at `verification.py:199`, so `setdefault` is a no-op. Specify overwriting/merging the `backend` entry (ok=False plus the contract-violation message) so exit 1 is explained, per the step\'s \'report every violation\' goal and the AC.', "step_7.md:37 — Add explicit implementation coverage for the acceptance criterion '`verify` flags when `OPENAI_API_KEY` overrides a configured `api_key`': extend `_resolve_api_key` (`verification.py:56`) to report that a config `api_key` existed and was overridden (not just the winning source), and specify the resulting verify finding.", "step_9.md:35 — Preserve the ollama `api_key` value text when the hand-rolled case is removed: specify that the row renders 'not set (optional)' rather than falling through to `_mask_api_key(None)` → literal 'None', and strengthen TDD case 5 to assert the rendered value text, not just `ok=True`.", 'step_8.md:32 — Specify the call-site gate for the base-url shape check: state whether it stays under `if backend == "openai"` (`verification.py:260`) or is broadened to cover the `OLLAMA_HOST` redirect, and if broadened, define how the `/v1` heuristic avoids warning on valid ollama hosts such as `http://localhost:11434`.', 'step_7.md:34 — State explicitly where `verify.py` obtains the langchain `config` and `key_source`: both `_load_langchain_config` and `_resolve_api_key` are private llm-layer helpers, and the current plan implies a third `_load_langchain_config()` call per verify run. Define the exposed accessor or the passing mechanism.', 'step_15.md:19 — Resolve the dead public API: either give `get_system_prompt_path()` a consumer in a step, or drop it from the plan (step 16 uses `is_prompt_configured_but_missing`); vulture runs in this repo.', 'step_15.md:45 — Make the `_warned_paths` module-level cache test-safe: specify a reset fixture (or equivalent isolation) so TDD cases 2–4 asserting warning counts are not order-dependent under `-n auto`.', "step_1.md:5 — Drop the 'atomic — it cannot be split' claim and split step 1: separate the `endpoint_shape` key/label rename (which the issue outline itself calls separable) from the rest, per the 'split large steps' planning principle; ~250 matches across 10 src + 22 test files in one commit is too large."], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-23
**Findings**:
I'll gather context now.`pr_info/steps/step_5.md:70` — high — mode/backend key confusion in the api_key presence check: the ALGORITHM passes `mode` to `_is_present` while HOW writes `_API_KEY_ENV[backend]`, and `_API_KEY_ENV` has no `"azure"` entry — implemented with `mode`, an Azure config whose key comes from `OPENAI_API_KEY` yields a false `api_key` required-error and `_create_chat_model` raises `ValueError`, breaking a working env-var Azure setup (the exact regression Decision 6 exists to prevent).

`pr_info/steps/step_8.md:48` — high — the rebased shape check only skips `api_version` and `"n/a"`, not the unverified fallback: when construction fails (backend package missing, or a contract violation) and config `base_url` is unset, `target.url` is `"(not configured)"`, so the malformed-URL heuristic fires and prints `[WARN] (not configured) — malformed URL; use e.g. https://host/v1`; today the check is silent in that case.

`pr_info/steps/step_5.md:43` — medium — `mode_of()` leaks the internal mode namespace into the user-facing backend namespace: a literal `backend = "azure"` resolves to `_CONTRACT["azure"]` and validates clean, so `verify` renders `Backend [OK] azure` with no contract finding while `_create_chat_model` still dies on the old `Unsupported langchain backend: 'azure'` — defeating step 9's unsupported-backend row for that value.

`pr_info/steps/summary.md:223` — medium — "Steps 3, 14, 17, 18 are independent" is wrong for step 3, and step 2's dependency is unlisted: step 3's "retired env var is set and ignored" message is false until step 1 stops reading `MCP_CODER_LLM_LANGCHAIN_ENDPOINT`, and step 2's `endpoint` rename hint is untestable until step 1 removes `endpoint` from the schema (it never reaches the unknown-key branch before then).

`pr_info/steps/summary.md:219` — medium — the ordering-constraint list omits 7 before 9: step 9's ALGORITHM unpacks `key, src, _over = _resolve_api_key(...)`, the 3-tuple introduced in step 7, so step 9 does not compile against the step-6 tree.
**Decisions**:
Verdict(decision='tasks', tasks=['step_5.md:70 — Resolve the mode/backend key confusion in the api_key presence check: the ALGORITHM passes `mode` to `_is_present` while HOW writes `_API_KEY_ENV[backend]`, and `_API_KEY_ENV` has no `"azure"` entry. Specify one consistent key (and add the azure entry / OPENAI_API_KEY mapping if mode is used) so an Azure config keyed off `OPENAI_API_KEY` does not raise a false required-error in `_create_chat_model`, per Decision 6.', 'step_8.md:48 — Extend the shape-check skip list to cover the unverified fallback: when construction fails and config `base_url` is unset, `target.url` is `"(not configured)"` and the malformed-URL heuristic wrongly warns. Specify that the sentinel (alongside `api_version` and `"n/a"`) suppresses the check so behaviour stays silent in that case.', 'step_5.md:43 — Stop `mode_of()` leaking the internal mode namespace into the user-facing backend namespace: a literal `backend = "azure"` must not resolve to `_CONTRACT["azure"]` and validate clean. Specify that only supported backend values map to contracts, so `verify` reports the unsupported-backend row (step 9) instead of `Backend [OK] azure` while `_create_chat_model` still raises `Unsupported langchain backend: \'azure\'`.', 'summary.md:223 — Correct the independence claim: step 3 depends on step 1 (its "retired env var is set and ignored" message is false until step 1 stops reading `MCP_CODER_LLM_LANGCHAIN_ENDPOINT`), and step 2 depends on step 1 (the `endpoint` rename hint is untestable until `endpoint` is removed from the schema). Update the list accordingly.', "summary.md:219 — Add the missing ordering constraint 7 before 9: step 9's ALGORITHM unpacks the 3-tuple `key, src, _over = _resolve_api_key(...)` introduced in step 7, so step 9 does not compile against the step-6 tree."], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-08-23
**Findings**:
I'll gather context now.`pr_info/steps/step_6.md:88` — high — `_source_for` names the first *set* redirect env var as the URL's provenance without checking that it produced the URL; with plain `openai`, no config `base_url` and a stale `AZURE_OPENAI_ENDPOINT` exported, the echo reports `AZURE_OPENAI_ENDPOINT env var` as the source of the SDK-default `https://api.openai.com/v1/` — a fabricated provenance claim, the exact class of bug this issue exists to remove. The env var's value must match `target.url` (and `AZURE_OPENAI_ENDPOINT` only applies in Azure mode).
`pr_info/steps/step_7.md:69` — high — same root cause in the `base_url_redirect` row: the stated condition is "`redirect_env_in_effect(backend)` is set **and** config `base_url` is unset or unmatched", so a stale `AZURE_OPENAI_ENDPOINT` (or `OPENAI_API_BASE` when `OPENAI_BASE_URL` actually won) prints `X overrides config.toml — requests go to https://api.openai.com/v1/` when nothing redirected anything; the row must be keyed on the var whose value equals `target.url`.
`pr_info/steps/step_9.md:68` — high — when `mode_of()` returns None (typo'd backend such as `opnai`, or `backend` unset) `validate()` emits only the `backend` finding, so `model` and `api_key` fall through to `default_ok=True` and a config with neither renders `Model [OK] None` and `API key [OK] not set (optional)` where both are `[ERR]` today — a diagnosability regression the TDD cases (5 covers ollama, 8 asserts only the `backend` row) cannot catch.
`pr_info/steps/step_9.md:70` — medium — HOW ("keep the masked value and `source` from `_resolve_api_key`; take `ok` from the finding") contradicts the ALGORITHM and DATA, where `_row_from` supplies both `ok` and `value` and the rendered row shows the finding's message as the value; the two readings produce different output for the `openai` + `base_url` + no-key case.
`pr_info/steps/step_5.md:92` — medium — Decision 5's "with `base_url` set the server may be unauthenticated, so missing `api_key` is a warning" is unverified against the SDK: `create_openai_model` passes `api_key=None` to `ChatOpenAI`, whose `openai.OpenAI` client raises when neither the argument nor `OPENAI_API_KEY` is set; if that holds, the unauthenticated-relay config cannot construct at all, `resolve_target` degrades to `unverified` for the issue's motivating setup, and the warning text gives wrong advice.
**Decisions**:
Verdict(decision='tasks', tasks=["step_6.md:88 — Fix `_source_for` so it does not fabricate provenance: require that the candidate redirect env var's value actually matches `target.url` (and that `AZURE_OPENAI_ENDPOINT` is only considered in Azure mode) before naming it as the source. A stale `AZURE_OPENAI_ENDPOINT` with plain `openai` and no config `base_url` must not be reported as the source of the SDK-default `https://api.openai.com/v1/`.", "step_7.md:69 — Key the `base_url_redirect` row on the env var whose value equals `target.url`, not merely on `redirect_env_in_effect(backend)` being set: a stale `AZURE_OPENAI_ENDPOINT`, or `OPENAI_API_BASE` when `OPENAI_BASE_URL` actually won, must not print 'X overrides config.toml — requests go to ...' when no redirect took effect.", "step_9.md:68 — Prevent the diagnosability regression when `mode_of()` returns None (typo'd or unset `backend`): `model` and `api_key` must not fall through to `default_ok=True`. Specify that with no resolvable mode those rows still render `[ERR]` as they do today, and extend the TDD cases (case 8 currently asserts only the `backend` row) to cover a config with backend typo and neither `model` nor `api_key` set."], escalate_reason=None)
**Changes**:
applied

## Round 4 — 2026-08-23
**Findings**:
I'll gather context now.`pr_info/steps/step_5.md:91` — high — the `openai` + `base_url` + missing-`api_key` rule (`ok=None`, "fine if the server at base_url is unauthenticated") is contradicted by the installed SDK: `create_openai_model` passes `api_key=None`, langchain's `validate_environment` still builds `openai.AsyncOpenAI(api_key=None, ...)` unconditionally (`root_client` is left `None`, the async branch is not guarded), and `AsyncOpenAI.__init__` raises `OpenAIError: Missing credentials...` under `_enforce_credentials=True` — so that config cannot construct a client at all; the plan would emit an exit-neutral warning plus wrong advice for a setup that is certainly broken, and `_create_chat_model` then fails with the opaque SDK message the issue exists to replace.

`pr_info/steps/step_6.md:70` — medium — "Construction can fail for two legitimate reasons: the backend package is not installed, or the step-5 contract is violated" is incomplete: a contract-*clean* openai config with `base_url` set and no api_key anywhere also raises (see above), so `resolve_target` silently degrades to the `unverified` fallback for exactly the unauthenticated-relay case the effective-config echo is meant to explain.

`pr_info/steps/step_6.md:110` — medium — for a default `ollama` config (no `base_url`, no `OLLAMA_HOST`) `create_ollama_model` omits the `base_url` kwarg, so `ChatOllama.base_url` is unset and `dialed_url()` returns `None`; the algorithm then yields `ResolvedTarget("(unknown)", "SDK default", True)` and the echo prints `base_url (unknown)` for the most common ollama setup, where the truthful value (`http://localhost:11434`) is knowable from `_resolve_ollama_host`.

`pr_info/steps/step_9.md:36` — medium — HOW ("keep the masked value and `source` from `_resolve_api_key`; take `ok` from the finding") still contradicts the ALGORITHM, where `_row_from(findings.get("api_key"), ...)` supplies both `ok` and `value` from the finding; the two readings render different `API key` rows for the `openai` + `base_url` + no-key case (masked key vs. the finding message).
**Decisions**:
Verdict(decision='tasks', tasks=['step_5.md:91 — Correct the `openai` + `base_url` + missing-`api_key` rule: the reviewer verified against the installed SDK that langchain\'s `validate_environment` unconditionally builds `openai.AsyncOpenAI(api_key=None, ...)`, which raises `OpenAIError: Missing credentials`, so this config can never construct a client. Replace the `ok=None` warning and the "fine if the server at base_url is unauthenticated" advice with a hard `ok=False` contract error whose message names the missing `api_key`/`OPENAI_API_KEY`, so `_create_chat_model` fails with the actionable message instead of the opaque SDK error; update Decision 5 and the corresponding TDD case accordingly.'], escalate_reason=None)
**Changes**:
applied

## Round 5 — 2026-08-23
**Findings**:
I'll gather context first: knowledge base, the issue tree, and the plan files.`pr_info/steps/step_5.md:37` — high — the `azure` row of `_API_KEY_ENV` maps only `OPENAI_API_KEY`, but `create_openai_model` passes `api_key=None` through to `openai.AzureOpenAI.__init__`, which resolves `AZURE_OPENAI_API_KEY` (and `AZURE_OPENAI_AD_TOKEN`) itself — verified in the installed SDK; a working Azure setup keyed off those variables gets a false `api_key` required-error, `_create_chat_model` raises `ValueError` and `verify` exits 1, the exact Decision-6 regression class the step's own note guards against for `OPENAI_API_KEY`

`pr_info/steps/step_5.md:38` — high — the `gemini` row maps only `GEMINI_API_KEY`, but `create_gemini_model` omits the `google_api_key` kwarg when neither config nor `GEMINI_API_KEY` yields a key, leaving the SDK to resolve `GOOGLE_API_KEY` (its primary variable, `secret_from_env(["GOOGLE_API_KEY", "GEMINI_API_KEY"])`) or Vertex ADC/`credentials`; those configs work today and would now hard-fail with a contract `ValueError` plus exit 1

`pr_info/steps/step_7.md:24` — high — `describe_effective_config(config, target, api_key_source)` receives neither the resolved key nor `overridden`, yet the ALGORITHM's undefined `masked` (line 117) and the DATA line `Qwen...abcd (from OPENAI_API_KEY env var — overrides config.toml api_key)` (line 135) require both; as specified the builder can only mask `config["api_key"]` — printing one source's value under another source's label (fabricated provenance) — or print `(not set)` whenever the key comes from the env var

`pr_info/steps/step_9.md:36` — high — HOW ("keep the masked value and `source` from `_resolve_api_key`; take `ok` from the finding") still contradicts the ALGORITHM, where `_row_from(findings.get("api_key"), ...)` supplies both `ok` and `value`; under the HOW reading the missing-key case renders `API key [ERR] not set (optional)` and the contract message naming `api_key`/`OPENAI_API_KEY` never reaches the user, defeating the acceptance criterion "exit 1, naming the contract violation" (unresolved since round 3)
**Decisions**:
Verdict(decision='escalate', tasks=[], escalate_reason='Final round with four unresolved high findings — incorrect API-key env-var contract tables for azure/gemini that would hard-fail working setups, a describe_effective_config signature gap, and the step-9 HOW/ALGORITHM contradiction outstanding since round 3 — and no rounds remain to apply fixes.')
**Changes**:
escalate
**Escalate reason**: Final round with four unresolved high findings — incorrect API-key env-var contract tables for azure/gemini that would hard-fail working setups, a describe_effective_config signature gap, and the step-9 HOW/ALGORITHM contradiction outstanding since round 3 — and no rounds remain to apply fixes.
