# review-plan review log 1

## Round 1 — 2026-08-17
**Findings**:
I'll gather context now.`pr_info/steps/summary.md:124` — high — `test_langchain_agent_usage.py` is listed as "no edit expected", but its three tests call `run_agent` with `mock_agent.ainvoke.return_value` and assert `stats["usage"]` derived from message `usage_metadata`; after step 5's drainer collapse `ainvoke` is never called and usage comes from `on_chat_model_end` events, so all three fail unless rewritten.
`pr_info/steps/summary.md:76` — high — "no code changes, existing usage tests stay green" is false for `test_langchain_agent_usage.py`: moving `run_agent` usage from the final message list to the stream's `on_chat_model_end` accumulator is a real source change for the non-stream path, not a deliberate non-change.
`pr_info/steps/step_5.md:83` — high — the "Tests (update first)" list omits `test_langchain_agent_usage.py`, so step 5 as written cannot exit green; those tests need `astream_events` mocks plus `on_chat_model_end` events to keep asserting summed usage.
`pr_info/steps/step_4.md:71` — high — the root-`run_id` terminal-`on_chain_end` assumption is exercised only by `graph_events()` fixtures the plan itself fabricates; if real LangGraph emits a different shape the guard logs a warning and silently never persists, losing icoder multi-turn history. The existing `tests/llm/providers/langchain/test_langchain_integration.py::TestAgentModeIntegration` (agent + session-continuity, real endpoint) would cover this after step 5 but is never referenced or required to run.
`pr_info/steps/step_4.md:51` — high — "the stats loop moved verbatim out of `run_agent`" contradicts `step_4.md:8` and `step_4.md:154` ("Do NOT touch run_agent in this step"); it must be copied in step 4 and deleted in step 5, otherwise step 4 breaks `run_agent`.
`pr_info/steps/step_4.md:81` — medium — the no-terminal-event `done` pseudocode (`done(messages=[], result="", stats=EMPTY)`) drops `session_id` and `usage`, but `step_4.md:117` requires the three `TestRunAgentStreamUsage` tests (which emit no terminal event) to pass unchanged asserting `done["usage"]`.
`pr_info/steps/step_5.md:72` — medium — "`wait_for` preserves today's timeout semantics" is inaccurate: today `asyncio.wait_for` wraps only `agent.ainvoke` (`agent.py:407`), while the drainer wraps MCP tool discovery too, so tool loading now counts against `timeout`.
`pr_info/steps/summary.md:33` — medium — sessions stored by the current agent code embed `SystemMessage` entries; on resume (`--session-id` / `--continue-session`) `assemble_messages` places the fresh merged system before them, so those legacy systems are non-leading in the sent list and the provider still rejects turn 1. No migration or documented behaviour for pre-existing histories.
`pr_info/steps/summary.md:120` — low — `tests/llm/providers/langchain/test_tool_build_helper.py` drives `run_agent_stream` (and patches `store_langchain_history`) but appears in neither the modified nor the verified-unchanged table.
**Decisions**:
Verdict(decision='tasks', tasks=["In pr_info/steps/step_5.md, add `tests/llm/providers/langchain/test_langchain_agent_usage.py` to the 'Tests (update first)' list at line ~83, specifying that its three tests must be rewritten to mock `astream_events` and emit `on_chat_model_end` events so usage assertions still hold after the drainer collapse.", "In pr_info/steps/summary.md, correct the line ~124 table entry for `test_langchain_agent_usage.py`: it is not 'no edit expected' — mark it as modified in step 5.", "In pr_info/steps/summary.md, fix the claim at line ~76 that step 5 has 'no code changes, existing usage tests stay green' — state explicitly that moving `run_agent` usage accounting from the final message list to the stream's `on_chat_model_end` accumulator is a real source change to the non-stream path.", "Resolve the contradiction between pr_info/steps/step_4.md:51 ('stats loop moved verbatim out of run_agent') and step_4.md:8 / step_4.md:154 ('Do NOT touch run_agent in this step'): state that the stats loop is COPIED into the drainer in step 4 and DELETED from `run_agent` in step 5, so step 4 leaves `run_agent` working.", 'In pr_info/steps/step_4.md, require `tests/llm/providers/langchain/test_langchain_integration.py::TestAgentModeIntegration` (agent + session-continuity against the real endpoint) to be run as the validation gate for the root-`run_id` terminal-`on_chain_end` assumption, rather than relying solely on fabricated `graph_events()` fixtures — the silent-warning fallback would otherwise lose icoder multi-turn history undetected.', 'In pr_info/steps/step_4.md, fix the no-terminal-event `done` pseudocode at line ~81: it must still carry `session_id` and the accumulated `usage` (not `stats=EMPTY`), so the three `TestRunAgentStreamUsage` tests required to pass unchanged at step_4.md:117 can assert `done["usage"]`.', "In pr_info/steps/step_5.md line ~72, correct the claim that `wait_for` preserves today's timeout semantics: today `asyncio.wait_for` wraps only `agent.ainvoke` (agent.py:407), whereas the drainer also wraps MCP tool discovery, so tool loading now counts against `timeout` — document this as an intentional behaviour change or scope the `wait_for` to exclude tool discovery.", 'In pr_info/steps/summary.md around line 33, document the resume path for pre-existing histories: sessions written by the current agent code contain `SystemMessage` entries, and on `--session-id` / `--continue-session` `assemble_messages` puts the fresh merged system ahead of them, leaving legacy systems non-leading so the provider still rejects turn 1. Specify either a migration (strip legacy system messages on load) or the documented accepted behaviour.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-17
**Findings**:
I'll gather context now.`pr_info/steps/step_4.md:88` — high — the no-terminal-event branch emits `result: ""` / `messages: []`, and step 5's drainer reads `final_text` off exactly that key, so a failed root-`run_id` capture silently turns the non-stream agent path (`_ask_agent` → `run_agent`) into an empty-text response; today `final_text` comes straight from `ainvoke` and cannot be lost. `accumulated_text` (deleted at `step_4.md:51`) is the obvious `result` fallback and should be kept.
`pr_info/steps/step_6.md:35` — high — the agent-path rejection test drives `run_agent_stream` directly with hand-built `system_messages` and hand-fed turn-2 history, so it never exercises `_build_system_messages` (the merge), `_ask_agent_stream`, or the real `load_langchain_history` round-trip; the issue's acceptance criterion asks for the symptom reproduced "with a system + project prompt" on the path that actually failed (icoder), which no automated test in the plan covers end-to-end.
`pr_info/steps/summary.md:104` — medium — "`ResponseAssembler` is not touched … size note only" is incomplete: `ResponseAssembler.add` consumes `done["result"]` (`src/mcp_coder/llm/types.py:141-143`) as the text fallback when no `text_delta` was seen, so adding `result` to the agent `done` event changes the assembled `text` for streaming agent runs that emit no token deltas — an undocumented, untested semantic change.
`pr_info/steps/summary.md:106` — medium — `done.messages` is the *whole* serialized conversation and is appended to `raw_response["events"]` on every icoder turn, so stored session JSON grows quadratically with turn count rather than "roughly doubles"; the payload is needed only by the non-stream drainer.
`pr_info/steps/summary.md:139` — low — the Modified table says `test_langchain_agent_system_messages.py` gets "Merged assertions", contradicting `step_2.md:66-68` ("needs **no** change here") and `step_5.md:134-139`, which lists only `astream_events` mock changes.
`pr_info/steps/step_4.md:131` — low — "cancel persists nothing" is gated only on `final_messages is None`; `run_agent_stream` checks `cancel_event` at the top of the loop (`agent.py:569`), so a cancel signalled after the terminal `on_chain_end` was captured still stores, and `test_cancel_persists_nothing` (cancelling before the terminal event) would not catch it — gate the store on `cancel_event` explicitly.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_4.md around line 88, change the no-terminal-event fallback so `done` carries the accumulated text as `result` instead of `""`: keep `accumulated_text` (currently slated for deletion at step_4.md:51) and use it as the `result` fallback, so a failed root-`run_id` capture cannot silently turn the non-stream agent path (`_ask_agent` -> `run_agent`, which reads `final_text` off that key in step 5) into an empty-text response. Note the corresponding test that asserts non-empty `result` in the no-terminal-event case.', "In pr_info/steps/step_6.md, add an end-to-end test for the agent-path system-message rejection that goes through `_build_system_messages` (the merge), `_ask_agent_stream`, and a real `store_langchain_history` / `load_langchain_history` round-trip for turn 2 — rather than driving `run_agent_stream` directly with hand-built `system_messages` and hand-fed history — so the issue's acceptance criterion ('reproduce with a system + project prompt' on the icoder path) is actually covered.", 'In pr_info/steps/summary.md around line 104, correct the \'`ResponseAssembler` is not touched ... size note only\' claim: `ResponseAssembler.add` consumes `done["result"]` (src/mcp_coder/llm/types.py:141-143) as the text fallback when no `text_delta` was seen, so adding `result` to the agent `done` event changes assembled `text` for streaming agent runs with no token deltas. Document the intended semantics and add a test covering the no-`text_delta` streaming agent case.', 'In pr_info/steps/summary.md around line 106, fix the growth claim: `done.messages` is the whole serialized conversation appended to `raw_response["events"]` on every icoder turn, so stored session JSON grows quadratically with turn count, not \'roughly doubles\'. Since the payload is only needed by the non-stream drainer, specify that `done.messages` is excluded from the persisted `raw_response["events"]` (or otherwise bounded), and state the chosen behaviour explicitly.'], escalate_reason=None)
**Changes**:
applied
