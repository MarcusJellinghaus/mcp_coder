# review-plan review log 1

## Round 1 — 2026-08-30
**Findings**:
I'll gather context now — knowledge base, the issue tree, and the plan files.Reading the knowledge base and the rest of the issue in parallel.Now verifying the plan's key claims against the actual codebase.`pr_info/steps/step_2.md:124` — high — `detach()` only clears `_pending`; it never cancels the futures it drops, so a turn that ends while an approval is pending leaves the interceptor coroutine parked forever on an unresolvable future and leaks its daemon agent thread for the process lifetime (registry is gone, so `resolve_pending` can no longer reach it). `detach()` must cancel remaining futures via `call_soon_threadsafe`, or the GeneratorExit path must `cancel_all()` before detaching.
`pr_info/steps/summary.md:74` — high — "`GeneratorExit` is provably unreachable [while pending]" holds only while the consumer is inside `q.get`; after the `approval_request` is yielded the consumer sits suspended at `yield` while `call_from_thread(_handle_stream_event, …)` blocks the worker thread — under #1046 that spans the entire open modal — so any exception in the UI handler closes the generator with an approval still pending, reaching the `thread.join(timeout=5)` + `detach()` path. Same claim repeated at `pr_info/steps/step_8.md:55`.
`pr_info/steps/step_5.md:77` — high — `paused` is credited only in the `except queue.Empty` branch, so a human pause shorter than the 300s inactivity timeout contributes nothing and is charged in full against `_AGENT_OVERALL_TIMEOUT` (3600s); this breaks the "neither timeout trips during a human pause" criterion for the common case, and the prescribed test (tool blocks longer than both monkeypatched timeouts) cannot detect it. Timestamp the pending window instead of accumulating whole `timeout` units.
`pr_info/steps/summary.md:44` — medium — "everything that mutates the engine runs on the agent loop, single-threaded" is inaccurate: `attach()`/`detach()` mutate `_pending`/`_loop`/`_cancelled` from the consumer thread (and `detach()` runs after a `join(timeout=5)` that can expire with the agent loop still live), and `cancel_all()` sets `_cancelled` directly on the Textual thread (`pr_info/steps/step_2.md:110`). The plan mandates committing this rationale as the `ApprovalEngine` docstring, and the `finally: _emit_front()` needs an explicit `_emit is None` guard for the post-detach race.
`pr_info/steps/step_6.md:73` — medium — `pop_pending_tool` falls back to name-FIFO whenever the id lookup *misses*, not only when `run_id` is absent; langchain events always carry `tool_run_id` on both sides, so a miss silently mis-pairs — reintroducing the exact defect R1/R18 exist to fix. Gate the fallback on `run_id is None`.
`pr_info/steps/step_8.md:46` — medium — the interim auto-deny returns through the gateway's `_DENY_USER` wording ("Tool call denied by the user…", `pr_info/steps/step_4.md:22`) although no user was prompted; until #1046 lands every `ask`-gated call tells the model a user denied it. Needs a distinct reason for the unanswered case.
`pr_info/steps/step_8.md:1` — medium — Step 8 bundles the UI branch, cancel channel, `on_unmount`, six real-agent integration tests, three Textual pilot tests and the spike deletion into one commit; it is materially larger than every other step and mixes unrelated cleanup. Split into UI-wiring and end-to-end/cleanup commits.
`pr_info/steps/step_3.md:70` — medium — the runtime-as-a-group stage lets a broad runtime `always` shadow a more specific authored `never`; the plan records this as intended widening (test 2) but adds no bound (e.g. excluding `NEVER` candidates from the runtime contest) and no follow-up issue, on a change carrying the `security` label.
`pr_info/steps/step_1.md:10` — low — Step 1 drives a *local copy* of the `_ask_agent_stream` consumer, so it cannot regress production code; `pr_info/steps/summary.md:180` nevertheless bills the file as the "real-path cancel regression test", and no step converts it — the real-path assertion exists only in Step 8's integration test.
`pr_info/steps/step_7.md:59` — low — the R16 `cancelled` flag is sticky and cleared only by `attach()` inside the provider, while `action_cancel_stream` (documented as a no-op when not streaming) and `on_unmount` set it unconditionally; the lifecycle of a value read by `AppCore` is split across three layers and threads. Resetting it at the start of `AppCore.stream_llm` would keep it local.
`pr_info/steps/summary.md:202` — low — the modified-files table omits `tests/icoder/test_icoder_permission_wiring.py` (`pr_info/steps/step_7.md:20`) and `tests/llm/test_types.py` (`pr_info/steps/step_5.md:19`).
**Decisions**:
Verdict(decision='tasks', tasks=['pr_info/steps/step_2.md:124 — Change `detach()` so it cancels every future still in `_pending` (scheduling the cancellation onto the agent loop via `call_soon_threadsafe`) before clearing state, or require the `GeneratorExit` path to call `cancel_all()` prior to `detach()`; otherwise a turn ending with an approval pending parks the interceptor coroutine on an unresolvable future and leaks the daemon agent thread.', 'pr_info/steps/summary.md:74 — Remove the claim that `GeneratorExit` is unreachable while an approval is pending (and the repeat at pr_info/steps/step_8.md:55). Document that after `approval_request` is yielded the consumer is suspended at `yield` for the whole open-modal window, so a UI-handler exception closes the generator with a pending approval and reaches the `thread.join(timeout=5)` + `detach()` path; specify the required behaviour for that path.', 'pr_info/steps/step_5.md:77 — Replace the `except queue.Empty`-only accumulation of `paused` with a timestamped pending window (record when the approval starts, credit the elapsed wall time when it resolves) so pauses shorter than the 300s inactivity timeout are still excluded from `_AGENT_OVERALL_TIMEOUT`, and update the prescribed test to cover a pause shorter than the inactivity timeout.', "pr_info/steps/summary.md:44 — Correct the 'everything that mutates the engine runs on the agent loop, single-threaded' rationale (mandated as the `ApprovalEngine` docstring) to state accurately which members are touched off-loop: `attach()`/`detach()` mutating `_pending`/`_loop`/`_cancelled` from the consumer thread after a `join(timeout=5)` that may expire, and `cancel_all()` setting `_cancelled` from the Textual thread (pr_info/steps/step_2.md:110). Also add an explicit `_emit is None` guard to the `finally: _emit_front()` path for the post-detach race.", 'pr_info/steps/step_6.md:73 — Gate the name-FIFO fallback in `pop_pending_tool` on `run_id is None` only; when a `run_id` is supplied but not found, do not fall back (return None / surface the miss) so a lookup miss cannot silently mis-pair tool calls, which is the defect R1/R18 exist to fix.', "pr_info/steps/step_8.md:46 — Give the interim auto-deny its own reason string distinct from the gateway's `_DENY_USER` wording (pr_info/steps/step_4.md:22), so unanswered `ask`-gated calls are not reported to the model as user denials before #1046 lands.", 'pr_info/steps/step_3.md:70 — Bound the runtime-as-a-group widening so a broad runtime `always` cannot shadow a more specific authored `never` (e.g. exclude `NEVER` candidates from the runtime contest), or, if the widening is deliberate, record an explicit follow-up issue and state the bound in the plan given the change carries the `security` label.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-30
**Findings**:
I'll gather context systematically — knowledge base, issues, then plan files.`pr_info/steps/step_5.md:53` — high — `detach()` is placed "in the existing `finally` (next to `thread.join`)" without fixing the order; it must run **before** `thread.join(timeout=5)`, otherwise on the GeneratorExit-while-pending path the join always expires (5s burned) with the interceptor still parked, and Step 5's own test 5 assertion (`thread.is_alive() is False` after the 5s join, `step_5.md:146`) cannot hold.

`pr_info/steps/summary.md:51` — high — §2.2 states as settled design that `detach()` "follows `thread.join(timeout=5)`, and that join can expire with the loop still live", which directly contradicts `step_5.md:146` and the issue's "the agent thread **actually terminates** — assert `thread.is_alive()` is `False` after the existing 5s join" criterion; the same after-join assumption is repeated at `pr_info/steps/step_2.md:80` and `pr_info/steps/step_2.md:112`.

`pr_info/steps/summary.md:288` — high — Step 7 wires the engine into production (gateway + service + `AppCore`) while the UI `approval_request` branch, the interim fail-closed deny and the direct cancel channel only land in Step 8; between those two commits any `ask`-gated tool wedges the turn permanently — the pause suppresses both the 300s inactivity timeout and `_AGENT_OVERALL_TIMEOUT`, `_cancel_event` is inert (checked only after an event arrives, `ui/app.py:290`), and the worker runs on a non-daemon executor thread, so the app cannot be quit cleanly. Move the UI branch + cancel channel into Step 7 (or wire the engine in Step 8).

`pr_info/steps/step_8.md:127` — high — R9's criterion ("quitting with an approval pending … the process exits without stalling") is tested only as "unmounting calls `cancel_pending_approvals()`" — a proxy for the hook, not the exit. `App._shutdown()` dispatches `Unmount` after `_close_all()`/`_close_messages()`, and after the cancel unwinds the turn `_stream_llm`'s `finally` still issues `self.call_from_thread(self._reset_busy_indicator)` (`src/mcp_coder/icoder/ui/app.py:313`) on an app that is already shut down, where `call_from_thread` on a stopped loop can block the worker thread indefinitely. No step covers that window.

`pr_info/steps/step_5.md:52` — medium — "`approval_bridge.attach(q.put)` immediately after `q` is created, **inside the existing `try`**" is self-contradictory: `q` is created at `llm/providers/langchain/__init__.py:557`, ~33 lines above the `try:` (`:590`) whose `finally` holds the join, so attach as specified lands outside the try and a failure between attach and the loop (e.g. `thread.start()`) leaves the engine attached to a dead turn with no `detach()`.
**Decisions**:
Verdict(decision='tasks', tasks=["pr_info/steps/step_5.md:53 — Specify the ordering explicitly: `detach()` (with cancellation of any remaining pending futures) must run BEFORE `thread.join(timeout=5)` in the `finally`, not merely 'next to' it; otherwise the GeneratorExit-while-pending path burns the full 5s join with the interceptor still parked and Step 5's own test-5 assertion `thread.is_alive() is False` (step_5.md:146) cannot hold.", 'pr_info/steps/summary.md:51 — Fix §2.2: remove the settled-design claim that `detach()` follows `thread.join(timeout=5)` and that the join may expire with the loop live; it contradicts step_5.md:146 and the issue criterion that the agent thread actually terminates. Update the same after-join assumption at pr_info/steps/step_2.md:80 and pr_info/steps/step_2.md:112 to match the detach-before-join ordering.', 'pr_info/steps/summary.md:288 — Eliminate the broken intermediate commit: move the UI `approval_request` branch, the interim fail-closed deny and the direct cancel channel into Step 7 alongside the gateway/service/`AppCore` wiring (or defer the engine wiring to Step 8). As specified, any `ask`-gated tool between the two commits wedges the turn permanently — both timeouts are suppressed by the pause, `_cancel_event` is inert (checked only after an event arrives, ui/app.py:290), and the worker is a non-daemon executor thread, so the app cannot be quit cleanly.', "pr_info/steps/step_8.md:127 — Replace the R9 proxy assertion ('unmounting calls `cancel_pending_approvals()`') with a test that covers the actual exit path, and add the guard it requires: after `App._shutdown()` dispatches `Unmount` and the cancel unwinds the turn, `_stream_llm`'s `finally` still calls `self.call_from_thread(self._reset_busy_indicator)` (src/mcp_coder/icoder/ui/app.py:313) against an already-stopped loop, which can block the worker indefinitely. Specify a shutdown/closed-app check around that call and assert the process exits without stalling.", "pr_info/steps/step_5.md:52 — Correct the contradictory placement instruction: `q` is created at llm/providers/langchain/__init__.py:557, ~33 lines above the `try:` at :590 whose `finally` holds the join, so 'immediately after `q` is created, inside the existing `try`' cannot both hold. State a placement that keeps `approval_bridge.attach(q.put)` inside the try/finally that owns `detach()`, so a failure between attach and the loop (e.g. `thread.start()` raising) cannot leave the engine attached to a dead turn."], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-08-30
**Findings**:
I'll gather context now.`pr_info/steps/step_8.md:105` — high — R9 pilot test 8 ("quit with an approval pending") cannot reach the pending state it asserts on: Step 7's interim auto-deny resolves every `approval_request` synchronously inside `_handle_stream_event`, and `pr_info/steps/step_7.md:122` states outright that "no approval is ever left pending across a quit in this commit". No mechanism is specified for suppressing the auto-deny, so the sole coverage of the R9 acceptance criterion (and of the `on_unmount` hook + closed-app guard it justifies) is unwritable as described.
`pr_info/steps/step_5.md:56` — medium — widening the `try` upwards so `thread = threading.Thread(...)` and `thread.start()` move inside it puts `thread.join(timeout=5)` in a `finally` that can now run before the thread was ever started; a `thread.start()` failure — the exact case the widening is justified by — is then masked by `RuntimeError: cannot join thread before it is started`. Specify a started/alive guard on the join.
`pr_info/steps/step_2.md:97` — medium — the engine's fail-closed path returns `ApprovalDecision("deny", "once")` with no `reason`, so `gateway.py`'s `approved.reason or _DENY_USER` (`pr_info/steps/step_4.md:84`) reports it to the model as "Tool call denied by the user. Do not retry this call" although no user was asked — the same defect the round-1 `_DENY_NO_UI` fix removed from the UI path. Give the engine's internal deny its own reason (e.g. `_DENY_ASK`-equivalent).
`pr_info/steps/step_3.md:29` — medium — the `authored_never` bound triggers on *any* matching authored `never`, including a broad one that would lose the specificity contest; a config with `"never": ["mcp__s__*"]` plus a specific `"ask": ["mcp__s__t"]` therefore silently discards a `scope=session` grant on `mcp__s__t` (the runtime group is skipped, and the authored `ask` then wins on `Policy.rank`), narrowing R14 further than the stated intent. Key the bound on an authored `never` that would actually win the contest.
`pr_info/steps/step_8.md:99` — medium — integration case 7 asserts the `ui/app.py` `_DENY_NO_UI` wording from a `tests/llm/providers/langchain/` module that drives `_ask_agent_stream` directly with no UI in the loop; nothing on that path produces the UI auto-deny, so the test degenerates to "the gateway honours `decision.reason`", already covered by Step 4 test 2b and Step 7 test 8, while pulling `icoder.ui.app` (and Textual) into an llm-layer test.
**Decisions**:
Verdict(decision='tasks', tasks=["pr_info/steps/step_8.md:105 — Make R9 pilot test 8 ('quit with an approval pending') actually reachable: Step 7's interim auto-deny resolves every `approval_request` synchronously inside `_handle_stream_event`, and pr_info/steps/step_7.md:122 states no approval is ever left pending across a quit in that commit. Specify how the test reaches the pending state — e.g. Step 8's UI branch replacing the auto-deny with a real modal wait before this test is written, or an explicit hook/patch point that suppresses the auto-deny — so the R9 acceptance criterion, the `on_unmount` cancel hook and the closed-app guard on `call_from_thread` all have real coverage rather than an unwritable test."], escalate_reason=None)
**Changes**:
applied
