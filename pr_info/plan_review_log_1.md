# review-plan review log 1

## Round 1 — 2026-08-28
**Findings**:
I'll gather context now.`pr_info/steps/step_3.md:113` — high — the "mechanical" row misses ~35 `@patch("mcp_coder.cli.commands.<cmd>.resolve_execution_dir")` decorators (`tests/cli/commands/test_create_plan.py:33,90,117,148,191,238`, `test_create_pr.py` ×11, `test_implement.py` ×9, `test_review.py` ×4, `test_rebase.py` ×3, `test_check_branch_status.py:205,264`); after the rename these fail as AttributeError patch targets, and because the new idiom rebinds `project_dir` from the resolver's return, each mock's `return_value` now feeds the workflow call — so assertions like `mock_workflow.assert_called_once_with(123, test_project_dir, …)` and `mock_resolve_flags.assert_called_once_with(args, test_project_dir)` break unless every mock returns the real project dir or the patch is dropped
`pr_info/steps/step_3.md:109` — high — the `tests/cli/test_utils_context_root.py` row names only two tests, but collapsing `report_context_root` to one parameter breaks all eight two-argument calls (`:143, :159, :175, :204, :232, :262, :286, :299`); `test_report_warns_when_the_only_hit_is_outside_project_dir` (`:179`) and `test_report_warns_when_every_hit_is_outside_project_dir` (`:210`) are built on cwd ≠ project_dir and must be re-expressed via the stubbed finder, and `test_report_does_not_warn_without_project_dir` (`:290`) becomes unconstructible once there is no separate anchor
`pr_info/steps/step_3.md:111` — medium — the two retained tests live in `TestSubprocessCwdParameter`, which carries `@pytest.mark.claude_cli_integration` and the `require_claude_cli` fixture, so the mandated fast gate (`-m "not … claude_cli_integration …"`) never runs them; the step's acceptance ("suite green; the renamed file keeps the two tests") is unverified unless the step also runs that marker explicitly
`pr_info/steps/step_4.md:42` — low — section C lists the dead `cwd = str(execution_dir) if execution_dir else str(project_dir)` fallbacks at `task_processing.py:394`, `review/reviewer.py:132`, `:210`, `ci.py:549` but omits the same inline fallback at `task_processing.py:221-222`
`pr_info/steps/step_3.md:20` — low — the command-docstring Args entries documenting the removed flag (`create_plan.py:33`, `create_pr.py:33`, `implement.py:33`, `rebase.py:68`, `check_branch_status.py:174`) are not listed anywhere in step 3, and step 4's grep sweep is the only thing that would catch them
`pr_info/steps/step_3.md:69` — low — in `commit.py` the rebinding `project_dir = resolve_claude_cwd(project_dir)` also changes the path handed to `validate_git_repository` and the git operations from unresolved to `.resolve()`d; the step frames this purely as supplying the subprocess cwd resolve
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_3.md, expand the "mechanical" test-update row at line 113 to enumerate every @patch("mcp_coder.cli.commands.<cmd>.resolve_execution_dir") decorator that must be removed or retargeted after the rename (tests/cli/commands/test_create_plan.py:33,90,117,148,191,238; test_create_pr.py ×11; test_implement.py ×9; test_review.py ×4; test_rebase.py ×3; test_check_branch_status.py:205,264), and state explicitly that because the new idiom rebinds project_dir from the resolver\'s return value, each retained mock must return the real project dir so existing assertions (mock_workflow.assert_called_once_with(123, test_project_dir, ...), mock_resolve_flags.assert_called_once_with(args, test_project_dir)) still hold.', 'In pr_info/steps/step_3.md, correct the tests/cli/test_utils_context_root.py row at line 109 to cover all eight two-argument report_context_root call sites (:143, :159, :175, :204, :232, :262, :286, :299) rather than only two; add explicit instructions to re-express test_report_warns_when_the_only_hit_is_outside_project_dir (:179) and test_report_warns_when_every_hit_is_outside_project_dir (:210) via the stubbed finder now that cwd != project_dir is no longer expressible, and to delete or replace test_report_does_not_warn_without_project_dir (:290), which becomes unconstructible once the separate anchor parameter is gone.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-28
**Findings**:
I'll gather context now.`pr_info/steps/summary.md:147` — high — the "Not splittable further" claim is wrong, and step 4 (34 `src/` files, ~470 test references, `interface.py` + 23 signatures + `CIFixConfig` in one commit) violates "split large steps / one step = one commit"; a green split exists: 4a rewrites `prompt_llm`/`prompt_llm_stream` and retargets only the direct callers (`prompt.py` ×3, `verify.py:506`, `verify.py:97-105`, `llm_service.py:114`, `env_setup.py:112`, `commit_operations.py:166`, `task_processing.py:221/439/543`, `rebase.py:319`, `finalisation.py`, `task_tracker_prep.py:78`, `create_plan/core.py`, `create_pr/core.py`, `review/core.py:425`, `reviewer.py`, `ci.py`) to pass `project_dir=` plus the `inject_prompts` mapping — the 23 workflow `execution_dir` parameters stay in place and stay *used*, so pylint/mypy/pytest are green — and 4b then deletes those parameters, the dead `cwd = str(execution_dir) if …` fallbacks and `CIFixConfig.cwd`

`pr_info/steps/step_3.md:111` — high — the two retained tests live in `TestSubprocessCwdParameter`, which carries `@pytest.mark.claude_cli_integration` and the `require_claude_cli` fixture (`tests/integration/test_execution_dir_integration.py:226-229, 242, 293`), so the mandated gate (`-m "not … claude_cli_integration …"`) never executes them; the step's own acceptance ("suite green; the renamed file keeps the two tests") and the issue's central criterion ("subprocess `cwd` equals the resolved `project_dir` from any shell cwd") are therefore unverified — step 3 must add an explicit `run_pytest_check(markers=["claude_cli_integration"])` run, as steps 1 and 2 mitigate the analogous gap with a grep instruction

`pr_info/steps/step_4.md:177` — high — step 4 rewrites every layer those two tests span (`execute_prompt` → `prompt_llm_stream` → `stream_subprocess`) yet its "Verification beyond the gate" section never runs them either; the same explicit `claude_cli_integration` marker run is needed here, otherwise `test_claude_cwd_integration.py` can ship broken

`pr_info/steps/step_4.md:51` — medium — section E lists only command-level callers; the intra-workflow forwards that must lose an argument are not enumerated anywhere (`implement/core.py:128, 156, 306`, `create_pr/core.py:526`, `create_plan/core.py:613`, `review/core.py:178, 232, 280, 384, 485`, `task_processing.py:523, 591`, `rebase.py:547, 587`, `review/steps.py:127`), several of them positional — only `check_branch_status.py:432, 463` is called out

`pr_info/steps/step_4.md:37` — medium — `execution_dir` is the third *positional* parameter of `generate_commit_message_with_llm` (`commit_operations.py:66-70`), so deleting it shifts `mcp_config`/`settings_file` by one for any positional caller; step 1 flags exactly this hazard for langchain, step 4 lists this row as "pure deletion" with no equivalent warning

`pr_info/steps/step_5.md:53` — medium — `docs/repository-setup/claude-code.md:243-246` ("`_read_settings_allow` … reads `<execution_dir>/.claude/settings.local.json`") is falsified by step 2's rename, but step 5's WHAT list and LLM prompt never mention it and the Verification bullet greps only `--execution-dir`, which does not match `<execution_dir>`

`pr_info/steps/step_3.md:113` — low — `tests/integration/test_mcp_config_integration.py:122` carries `execution_dir=None` on an `argparse.Namespace`; it falls outside the `tests/cli/commands/test_*.py` glob of the mechanical row and is listed in no step, so a stale reference survives the "no `execution_dir` left" sweep
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/summary.md, remove the incorrect "Not splittable further" claim for step 4 and split it into two steps/commits per the reviewer\'s green split: 4a rewrites prompt_llm/prompt_llm_stream and retargets only the direct callers (prompt.py x3, verify.py:97-105 and :506, llm_service.py:114, env_setup.py:112, commit_operations.py:166, task_processing.py:221/439/543, rebase.py:319, finalisation.py, task_tracker_prep.py:78, create_plan/core.py, create_pr/core.py, review/core.py:425, reviewer.py, ci.py) to pass project_dir= plus the inject_prompts mapping, leaving the 23 workflow execution_dir parameters in place and used so pylint/mypy/pytest stay green; 4b then deletes those parameters, the dead `cwd = str(execution_dir) if ...` fallbacks and CIFixConfig.cwd. Update pr_info/steps/step_4.md accordingly.', 'In pr_info/steps/step_4.md, add to the "Verification beyond the gate" section an explicit run_pytest_check(markers=["claude_cli_integration"]) invocation, since step 4 rewrites every layer the retained cwd tests span (execute_prompt -> prompt_llm_stream -> stream_subprocess) and the mandated fast gate excludes that marker, so test_claude_cwd_integration.py would otherwise ship unverified.', 'In pr_info/steps/step_4.md section E, enumerate the intra-workflow forwarding call sites that must drop an argument, not just the command-level callers: implement/core.py:128, 156, 306; create_pr/core.py:526; create_plan/core.py:613; review/core.py:178, 232, 280, 384, 485; task_processing.py:523, 591; rebase.py:547, 587; review/steps.py:127 — noting which are positional. Currently only check_branch_status.py:432, 463 is called out.', 'In pr_info/steps/step_4.md at the generate_commit_message_with_llm row (line ~37), replace the "pure deletion" framing with the same positional-shift warning step 1 gives for langchain: execution_dir is the third positional parameter (commit_operations.py:66-70), so removing it shifts mcp_config/settings_file by one for any positional caller — instruct the implementer to audit and update all call sites accordingly.', "In pr_info/steps/step_5.md, add docs/repository-setup/claude-code.md:243-246 (the `_read_settings_allow` ... `<execution_dir>/.claude/settings.local.json` text falsified by step 2's rename) to the WHAT list and the LLM prompt, and broaden the Verification grep beyond `--execution-dir` to also match `execution_dir`/`<execution_dir>` so such references are caught."], escalate_reason=None)
**Changes**:
applied
