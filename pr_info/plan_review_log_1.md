# review-plan review log 1

## Round 1 — 2026-08-02
**Findings**:
I'll gather context in parallel: knowledge base files, the issue, and the plan files.Let me read the rest of the issue and the plan files in parallel.Now let me read the knowledge base files and all step files.Now the individual step files.Let me verify key code-level assumptions the plan makes. I'll read the referenced source files.The plan's gateway/service assumptions check out. Let me verify `app_core.py` and `command_registry.py`.Let me check the CLI wiring and skills registration that the plan heavily depends on.Let me verify the import-linter contract names the plan references.Let me get accurate line numbers for the plan sections I'll cite.`pr_info/steps/step_2.md:73` — medium — `base: Base = "none" if d_drop else tools_block.base` assigns `SkillToolsBlock.base` (typed `str | None` per step_1's dataclass) to a `Base` literal; won't pass mypy-strict without a `cast`/narrowing, contradicting the step's own "mypy strict clean" exit criterion (and skill_tools "imports nothing" forbids retyping the field to `Base | None`).
`pr_info/steps/step_5.md:36` — low — `permission_degraded=config.degraded` is read at the `AppCore(...)` call site, but `config` is local to the `provider=="langchain" and mcp_config` block in `cli/commands/icoder.py` and is out of scope there; a flag must be hoisted to outer scope (default `False`) or this NameErrors.
`pr_info/steps/step_3.md:63` — low — yielding `permission_warning` before the assembler/`emit("stream_event")` loop drops these events from the event log (today they are emitted from within the service and logged), an unacknowledged behavior change for resume/replay.
`pr_info/steps/step_2.md:55` — low — the shared arg-scoped warning text ("elevates whole tool until #1053") is emitted on the deny side too, where an arg-scoped token over-denies rather than elevates; the message is misleading for `deny`.
`pr_info/steps/step_2.md:13` — low — skill_tools/skill_frame are only added to `permissions_leaf_isolation` source_modules, whose forbidden_modules omit `icoder.skills` and `icoder.core`; the AC's claim that the contract guarantees "permissions/ imports nothing from icoder.skills/core" is therefore not actually enforced (code complies, but the verification story overstates the contract).
**Decisions**:
Verdict(decision='tasks', tasks=['step_2.md:73 — Fix `base: Base = "none" if d_drop else tools_block.base` so it passes mypy-strict without retyping SkillToolsBlock.base: narrow/validate `tools_block.base` (str | None) into a `Base` literal (or cast) at the skill_frame boundary, since skill_tools must import nothing and the step\'s exit criterion requires mypy-strict clean.', 'step_5.md:36 — Hoist a permission flag (default `False`) to the outer scope in cli/commands/icoder.py so `permission_degraded=config.degraded` at the AppCore(...) call site does not reference the `config` local that only exists inside the `provider=="langchain" and mcp_config` block; as written it NameErrors.', 'step_3.md:63 — Route the `permission_warning` through the assembler/`emit("stream_event")` event-log path instead of yielding it before the loop, so these events are not dropped from resume/replay logging; if the change is intentional, explicitly acknowledge the behavior change in the step.', 'step_2.md:13 — Reconcile the AC claim that the contract guarantees "permissions/ imports nothing from icoder.skills/core": either add `icoder.skills` and `icoder.core` to the forbidden_modules of the permissions_leaf_isolation contract so it is actually enforced, or reword the AC to match what the contract verifies.'], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-02
**Findings**:
I'll gather context. Let me start by reading the knowledge base, the issue, and the plan files in parallel.Let me read the rest of the issue and all knowledge base and plan files.Now let me read all the plan files.Now the remaining steps 3, 4, 5.Let me verify some key assumptions in the actual codebase — imports in the permissions leaf, the gateway API, and the degraded config flag.Let me check the leaf modules' imports and key API assumptions (gateway `begin_turn`, `PermissionConfig.degraded`, `RealLLMService.stream`).Let me verify the command-name/slash handling that Step 4's `handle_input` guard depends on.Let me verify `AppCore` internals that Steps 3–4 depend on (`_registry`, `handle_input`, `stream_llm`).Let me get precise line numbers for the findings I'll cite.Based on my review of the plan against issue #1061 (and its authoritative decision table), the knowledge base, and the actual codebase, here are my findings:

pr_info/steps/step_2.md:97 — medium — AC "Both blocks present → rich `tools:` wins, no runtime warning" (D14) is not enumerated in Step 2's TESTS list (lines 98–106); `build_frame`'s precedence of `tools_block` over `allowed_tools` (and the required silence) is left untested despite being an explicit acceptance criterion.

pr_info/steps/step_1.md:12 — low — Plan adds `mcp_coder.icoder.skills`/`mcp_coder.icoder.core` to `permissions_leaf_isolation`'s `forbidden_modules`, a deviation from issue D10 (which says only *join `source_modules`*) that is not flagged in the summary's deviation note; verified safe (no leaf module imports `core`/`skills`) and arguably needed to enforce the final AC, but undocumented as a deviation.

pr_info/steps/step_3.md:86 — low — AC "every turn installs a frame, `None` included — a skill frame never leaks into the following turn" has no enumerated test; the invoke-skill-then-plain-message sequence (frame installed, then `frame=None`) is not covered in Step 3's TESTS list.
**Decisions**:
Verdict(decision='tasks', tasks=["step_2.md — Add a test to Step 2's TESTS list covering AC D14: when both a rich `tools:` block and `allowed_tools` are present, `build_frame` must let `tools_block` win over `allowed_tools` AND emit no runtime warning; assert both the precedence and the silence.", "step_3.md — Add a test to Step 3's TESTS list for the invoke-skill-then-plain-message sequence: assert a skill frame is installed on the skill turn and that the following plain turn installs `frame=None`, proving a skill frame never leaks into the next turn."], escalate_reason=None)
**Changes**:
applied
