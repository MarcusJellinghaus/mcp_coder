# review-plan review log 1

## Round 1 — 2026-08-29
**Findings**:
I'll gather context from the knowledge base, the issue tree, and the plan files.`pr_info/steps/step_3.md:50` — medium — Doc wording attributes the `--continue-session-from` breakage to *claude* response filenames only; `prompt.py:107-111` uses `extract_langchain_session_id` (filename stem) for langchain too, so a **langchain** `--store-response` file (`.mcp-coder/responses/response_<ts>.json`) also yields a bogus id — the documented `--store-response` → `--continue-session-from` workflow will hard-error under langchain. As written the doc note would mislead users.

`pr_info/steps/summary.md:124` — medium — "Known consequences" lists custom-path resume only as `/some/copy/abc.json`; it omits that the standard, documented `--store-response` + `--continue-session-from` pairing under `--llm-method langchain` moves from silent blank-start to exit 1 (stem is `response_<ts>`, never a langchain session id). This consequence is not in the issue and is not acknowledged anywhere in the plan.

`pr_info/steps/summary.md:171` — low — Acceptance item "exits 1" is never asserted by any planned test; the plan relies solely on the pre-existing bare-`except` boundary at `cli/commands/prompt.py:238-243`, and no step adds a CLI-level assertion tying the guard to exit code 1.

`pr_info/steps/step_2.md:108` — low — The unconditional autouse fixture also neutralises the guard for the `langchain_integration`-marked resume tests in `tests/llm/providers/langchain/test_langchain_integration.py:90,195,229`, the only end-to-end resumes with a real history file, so the guard is never live on an end-to-end path.

`pr_info/steps/step_2.md:152` — low — Tests 3 and 4 assert on the private `_resolve_session_id` rather than through `ask_langchain`/`ask_langchain_stream`; the "successful resume stays silent" and "new session unaffected" acceptance items are verified at helper level only.

`pr_info/steps/summary.md:98` — low — The "real-filesystem test lives one directory up" rationale conflicts in framing with `pr_info/steps/step_1.md:92`, whose `base_dir` test already satisfies the issue's acceptance item verbatim ("via a tmp `base_dir`"); the summary implies step 1's test does not count.

`pr_info/steps/step_3.md:1` — low — Docs-only step of three 1-3 line edits, tightly coupled to step 2 and with no behavioural exit criterion; planning principles call for merging tiny/intertwined steps rather than spending a commit plus a full pylint/pytest/mypy cycle on a zero-code change.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_3.md:50, correct the doc note: the bogus-session-id problem is not limited to claude response filenames — prompt.py:107-111 derives the id from the filename stem via extract_langchain_session_id for langchain too, so a langchain --store-response file (.mcp-coder/responses/response_<ts>.json) also produces an invalid id. State explicitly that the documented --store-response -> --continue-session-from workflow under --llm-method langchain will now exit 1.', "In pr_info/steps/summary.md:124 'Known consequences', add the omitted consequence: under --llm-method langchain the standard documented --store-response + --continue-session-from pairing changes from a silent blank start to exit 1, because the stored filename stem is response_<ts> and never a langchain session id. Call out that this goes beyond the custom-path /some/copy/abc.json case named in the issue.", 'Add a CLI-level test that ties the guard to the acceptance item at pr_info/steps/summary.md:171: assert `mcp-coder prompt --continue-session-from <file with non-session-id stem>` actually exits with code 1 (via the bare-except boundary at cli/commands/prompt.py:238-243), rather than relying on that boundary implicitly.', 'Change the autouse guard-disabling fixture planned at pr_info/steps/step_2.md:108 so it does not neutralise the guard for the langchain_integration resume tests in tests/llm/providers/langchain/test_langchain_integration.py:90,195,229 — scope it to the unit tests that need it (opt-in rather than unconditional autouse) so at least one end-to-end resume path exercises the guard live.', 'Merge the docs-only step 3 (pr_info/steps/step_3.md) into step 2: it is three 1-3 line edits with no behavioural exit criterion and is tightly coupled to step 2, so per the planning principles it should not be a separate commit and quality-check cycle. Renumber/adjust summary.md references accordingly.'], escalate_reason=None)
**Changes**:
applied
