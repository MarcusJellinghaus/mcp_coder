# review-plan review log 1

## Round 1 — 2026-08-25
**Findings**:
I'll gather context before reviewing. Starting with the knowledge base, the issue, and the plan files.Now let me verify the plan's claims against the actual source.`pr_info/steps/step_4.md:120` — high — No automated test asserts the issue's primary acceptance criterion end-to-end (`--project-dir X` with no `--execution-dir` ⇒ subprocess `cwd == X`); Step 4 only updates mocked `resolve_execution_dir` call-argument assertions plus a `commit.py` resolver test. `tests/integration/test_execution_dir_integration.py:283-330` already provides the exact pattern (`assert options.cwd == ...`), and the plan touches that test only to rename it — add the positive case there.
`pr_info/steps/step_3.md:50` — medium — Wiring `report_context_root` inside `resolve_execution_dir` makes every call site emit OUTPUT-level lines and perform a root-ward filesystem walk, but the step contains no expected-impact analysis on existing tests, unlike the careful enumeration Step 2 does at `step_2.md:100-111`. Unpatched resolver reachers (`prompt.py`, `commit.py`, `icoder.py` and their command tests) are unassessed.
`pr_info/steps/step_3.md:116` — medium — Test 6 ("nothing anywhere → `[]`") is self-contradictory: the walk runs to the filesystem root, so the prescribed `== []` assertion is exactly the environment-dependent assertion the same sentence warns against. No boundary/injection mechanism is specified to make it deterministic.
`pr_info/steps/step_5.md:85` — medium — Same defect as above for verify test 4 ("none found when no CLAUDE.md exists anywhere up the chain"): unreliable without a stop-boundary, since `tmp_path` ancestors are outside the test's control.
`pr_info/steps/summary.md:286` — medium — `tach.toml`'s `mcp_coder.cli` `depends_on` block does not list `mcp_coder.prompts`, so the new `cli/utils.py → prompts.prompt_loader` import risks breaking `tach check`; no step schedules a `tach.toml` update. The stated justification ("`verify.py` already imports `prompt_loader`") concerns import direction, not the tach allowlist, and tach could not be executed here to confirm.
`pr_info/steps/step_5.md:51` — medium — The "hit lies outside `project_dir`" predicate is written twice and independently (here and `step_3.md:82`), so the run-time report and the `verify` report can drift on the one rule the issue calls load-bearing. Share one predicate.
`pr_info/steps/step_4.md:17` — low — `check_branch_status.py:331` calls `resolve_execution_dir` only inside the `if args.fix > 0:` block, so requirement 5's "once per run" report is absent for `check branch-status` without `--fix`. Step 4's table lists it as an ordinary one-line site without noting the conditional.
`pr_info/steps/summary.md:270` — low — Open decision, answered: keep the `claude_md_paths` variant. It satisfies the constraint's stated purpose with a smaller diff; its only cost is that the ancestor-walk termination logic now exists in both `is_claude_md` and `find_context_claude_md`. Not worth reworking Step 1 to match the issue literally.
`pr_info/steps/step_5.md:19` — low — `tests/cli/commands/test_verify.py` is already 727 lines; adding six tests crosses the repo's documented 750-line guidance. A sibling test module would avoid it.
**Decisions**:
Verdict(decision='tasks', tasks=['Add a positive end-to-end test in tests/integration/test_execution_dir_integration.py (extend Step 4) asserting that when --project-dir X is passed with no --execution-dir, the Claude subprocess options.cwd == X, following the existing pattern at lines 283-330; do not limit Step 4 to renaming that test and mocked resolve_execution_dir call-argument assertions.', 'In step_3.md:50, add an expected-impact analysis for wiring report_context_root inside resolve_execution_dir (OUTPUT-level lines plus root-ward filesystem walk on every call), enumerating affected existing tests for the unpatched resolver call sites prompt.py, commit.py and icoder.py and their command tests, in the same style as step_2.md:100-111.', "Specify a deterministic stop boundary (e.g. an injectable/overridable walk-termination root) for the ancestor walk, and rewrite both step_3.md:116 test 6 ('nothing anywhere -> []') and step_5.md:85 verify test 4 ('none found up the chain') to use it, so the == [] assertion no longer depends on directories above tmp_path.", 'Add a step (or extend an existing one) that updates tach.toml to include mcp_coder.prompts in the mcp_coder.cli depends_on list for the new cli/utils.py -> prompts.prompt_loader import, and run tach check to confirm.', "Factor the 'hit lies outside project_dir' predicate into a single shared helper used by both the run-time report (step_3.md:82) and the verify report (step_5.md:51), instead of specifying it independently in two places.", "In step_4.md:17, note that check_branch_status.py:331 calls resolve_execution_dir only inside the `if args.fix > 0:` block, and specify how requirement 5's once-per-run context report is emitted for `check branch-status` without --fix.", "Place the six new verify tests from step_5.md:19 in a new sibling test module rather than tests/cli/commands/test_verify.py, which is already 727 lines and would exceed the repo's 750-line guidance."], escalate_reason=None)
**Changes**:
applied

## Round 2 — 2026-08-25
**Findings**:
I'll start by gathering context from the knowledge base, the issue, and the plan files.`pr_info/steps/step_4.md:187` — high — The only automated proof of the issue's headline acceptance criterion (`test_prompt_command_defaults_cwd_to_project_dir`) is placed in `TestSubprocessCwdParameter`, which carries `@pytest.mark.claude_cli_integration` (`tests/integration/test_execution_dir_integration.py:224`) and the `require_claude_cli` fixture that skips when the CLI is absent; it is therefore excluded from CLAUDE.md's mandated per-step pytest invocation, and step_4.md:218 explicitly permits committing without having run it. Add a CLI-free assertion of the same behaviour (e.g. in `TestPromptExecutionDir`, `tests/cli/commands/test_prompt.py:706`, which patches `prompt_llm_stream` and needs no executable) so the default-cwd change is proven in the default gate.
`pr_info/steps/step_7.md:97` — medium — Step 7's own exit check ("every `--execution-dir` mention across `docs/` states the new default and the deprecation") contradicts its three-file WHERE scope: `docs/configuration/claude-code.md:126` and `docs/repository-setup/claude-code.md:193-208` both document the flag, and the latter justifies `--settings` partly by "the workflow was invoked from a parent directory" — a cause this change eliminates. Neither file appears in Step 7 or in `summary.md:260`.
`pr_info/steps/summary.md:126` — medium — The intended settings-merge side effect (`<project_dir>/.claude/settings.json` newly activating) is assigned to "the release note", but no step produces a release note and the repo has no CHANGELOG; the only carrier is Step 4's commit message, which is not user-facing documentation.
`pr_info/steps/step_3.md:154` — medium — The "unaffected: tests patch the resolver" analysis lists `test_check_branch_status.py` on the strength of `:167`/`:226` only. Step 4's hoist moves `resolve_execution_dir` onto the read-only path, where ~10 further tests in that file (`:104`, `:143`, `:279`, `:312`, `:355`, `:419`, `:458`, `:496`, `:531`, `:571`) do not patch it and will newly execute the real resolver, the real root-ward walk and the real logging with a nonexistent `project_dir` (`/test/project`); Step 4 does not restate the impact for the sites its hoist newly exposes.
`pr_info/steps/step_4.md:167` — low — The test sketch's parameter list (`self, mock_prepare_env, mock_execute_subprocess, require_claude_cli, tmp_path`) omits `monkeypatch`, while difference 2 at `:177` requires `monkeypatch.chdir`; copied verbatim the sketch raises `NameError`.
`pr_info/steps/step_4.md:122` — low — `tests/cli/commands/test_prompt.py:712` (`test_default_execution_dir_uses_cwd`, asserting `execution_dir == str(Path.cwd())`) becomes misleading for exactly the reason the step records for the integration test it renames — it passes only because that test's `project_dir=None`. It is not listed for a rename or comment fix.
`pr_info/steps/step_2.md:3` — low — "Depends on: nothing (Step 1 is independent of this one)" contradicts `summary.md`'s "Steps 1→4 are strictly ordered (each depends on the previous)".
**Decisions**:
Verdict(decision='tasks', tasks=['Add a CLI-free test proving the headline acceptance criterion (--project-dir X with no --execution-dir => subprocess cwd == X) in TestPromptExecutionDir at tests/cli/commands/test_prompt.py:706, which patches prompt_llm_stream and needs no Claude CLI, so the behaviour is covered by the default (non-integration) pytest gate rather than only by the claude_cli_integration-marked TestSubprocessCwdParameter.', "Extend Step 7's WHERE scope to include docs/configuration/claude-code.md:126 and docs/repository-setup/claude-code.md:193-208 (and update summary.md:260 accordingly), updating both to state the new default and the deprecation, and revise the --settings justification in the latter that cites 'the workflow was invoked from a parent directory'.", 'Assign the settings-merge side effect (<project_dir>/.claude/settings.json newly activating) to a concrete user-facing carrier in the docs step instead of an unwritten release note, since no step produces one and the repo has no CHANGELOG.', 'Restate the resolver-impact analysis in Step 4 for the call sites its hoist newly exposes in tests/check_branch_status tests: the ~10 tests at :104, :143, :279, :312, :355, :419, :458, :496, :531, :571 do not patch resolve_execution_dir and would run the real resolver, root-ward walk and logging against the nonexistent /test/project; specify how each is patched or otherwise handled.', 'Add monkeypatch to the test sketch parameter list at step_4.md:167, since difference 2 at :177 uses monkeypatch.chdir and the sketch as written would raise NameError.', 'List tests/cli/commands/test_prompt.py:712 (test_default_execution_dir_uses_cwd) in Step 4 for a rename or clarifying comment, for the same reason recorded for the integration test being renamed: it passes only because project_dir is None.', "Fix the dependency contradiction: step_2.md:3 says 'Depends on: nothing' while summary.md states Steps 1->4 are strictly ordered; make the two statements agree."], escalate_reason=None)
**Changes**:
applied

## Round 3 — 2026-08-25
**Findings**:
I'll gather context now: knowledge base, the issue tree, and the plan files.`pr_info/steps/step_3.md:85` — medium — Step 3 gates the `tach.toml` edit on a `run_tach_check` verdict ("drop the edit if tach says it was already permitted"), but tach is not installed in this environment (`run_tach_check` returns "tach is not available"), so the implementer cannot resolve the conditional; specify the fallback (keep the `mcp_coder.prompts` entry, note that `verify.py:27` already imports `prompts.prompt_loader` and that `run_lint_imports_check` passes) so the step is not blocked.
`pr_info/steps/step_4.md:101` — low — The `prompt.py` (and `icoder.py:49-54`) reorder moves `resolve_execution_dir` below the project-dir validation, so a nonexistent `--project-dir` now reports before an invalid `--execution-dir`; the step analyses this error-ordering change for `commit.py` only. Both paths return 1 and no existing test pins the order (`tests/cli/commands/test_prompt.py:845` uses `project_dir=None`), but the change should be stated where the other two are.
**Decisions**:
Verdict(decision='dismiss', tasks=[], escalate_reason=None)
**Changes**:
dismiss
