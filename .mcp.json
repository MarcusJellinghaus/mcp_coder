{
  "mcpServers": {
    "code-checker": {
      "type": "stdio",
      "command": "${MCP_CODER_VENV_DIR}\\Scripts\\mcp-tools-py.exe",
      "args": [
        "--project-dir",
        "${MCP_CODER_PROJECT_DIR}",
        "--python-executable",
        "${MCP_CODER_VENV_DIR}\\Scripts\\python.exe",
        "--venv-path",
        "${MCP_CODER_VENV_DIR}",
        "--test-folder",
        "tests",
        "--log-level",
        "INFO"
      ],
      "env": {
        "PYTHONPATH": "${MCP_CODER_PROJECT_DIR}/src"
      }
    },
    "workspace": {
      "type": "stdio",
      "command": "${MCP_CODER_VENV_DIR}/Scripts/mcp-workspace.exe",
      "args": [
        "--project-dir",
        "${MCP_CODER_PROJECT_DIR}",
        "--log-level",
        "INFO",
        "--reference-project",
        "p_workspace=${USERPROFILE}\\Documents\\GitHub\\mcp-workspace",
        "--reference-project",
        "p_config=${USERPROFILE}\\Documents\\GitHub\\mcp-config",
        "--reference-project",
        "p_tools=${USERPROFILE}\\Documents\\GitHub\\mcp-tools-py"
      ],
      "env": {
        "PYTHONPATH": "${MCP_CODER_VENV_DIR}\\Lib\\"
      }
    }
  }
} 